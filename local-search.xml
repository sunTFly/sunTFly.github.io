<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>AI 大模型模型部署系列（三）：从 Chat Template 到多轮对话，理解 KV Cache 复用</title>
    <link href="/2026/08/29/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%89%EF%BC%89%EF%BC%9A%E4%BB%8EChat-Template%E5%88%B0%E5%A4%9A%E8%BD%AE%E5%AF%B9%E8%AF%9D/"/>
    <url>/2026/08/29/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%89%EF%BC%89%EF%BC%9A%E4%BB%8EChat-Template%E5%88%B0%E5%A4%9A%E8%BD%AE%E5%AF%B9%E8%AF%9D/</url>
    
    <content type="html"><![CDATA[<p>在<a href="https://suntfly.github.io/2026/08/26/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E4%BB%8ELogits%E5%88%B0Token/">上一篇文章《AI 大模型模型部署系列（二）：从 Logits 到 Token，理解 llama.cpp 采样链》</a>中，我们从 Logits 出发，验证了 Top-K、Top-P、Temperature、Seed 和最终选择器如何共同决定下一个 Token。</p><p>但是，能够连续生成 Token 还不等于能够正确对话。Instruct 模型需要知道哪段内容来自 System、哪段来自 User、什么时候轮到 Assistant 回答；进入第二轮后，程序还需要让消息历史与 KV Cache 保持同步，避免重复计算已经处理过的 Token。</p><p>本次以 <code>examples/simple-chat</code> 为入口，将单轮生成循环扩展为多轮对话程序，并重点处理三个问题：Chat Template 如何组织消息、第二轮为什么只 Decode 增量 Prompt、文本历史与 KV Cache 如何对应。</p><blockquote><p>llama.cpp 的接口变化较快。本文对应本地学习分支的基础版本 <code>bdbfb4c79</code>，其他版本的接口名称和实现细节可能不同。</p></blockquote><h2 id="一、从-Prompt-字符串到结构化消息"><a href="#一、从-Prompt-字符串到结构化消息" class="headerlink" title="一、从 Prompt 字符串到结构化消息"></a>一、从 Prompt 字符串到结构化消息</h2><p>普通补全接口接收一段字符串，而聊天程序维护的是一组有角色的消息：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">llama_chat_message</span> &#123;<br>    <span class="hljs-type">const</span> <span class="hljs-type">char</span> * role;<br>    <span class="hljs-type">const</span> <span class="hljs-type">char</span> * content;<br>&#125;;<br></code></pre></td></tr></table></figure><p>一轮对话通常包含三类角色：</p><ul><li><code>system</code>：规定助手的身份、边界和整体行为。</li><li><code>user</code>：用户本轮输入。</li><li><code>assistant</code>：模型已经生成的历史回答。</li></ul><p>这些角色并不会直接送入 Transformer。它们首先经过模型自己的 Chat Template，被序列化为训练时使用的文本格式，然后再 Tokenize。</p><p>以本次使用的 Qwen2.5 Instruct 模型为例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">&lt;|im_start|&gt;system<br>你是一名专业的 C++ 开发助手。&lt;|im_end|&gt;<br>&lt;|im_start|&gt;user<br>记住数字9527&lt;|im_end|&gt;<br>&lt;|im_start|&gt;assistant<br></code></pre></td></tr></table></figure><p>完整数据流是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">messages(role, content)<br>→ llama_chat_apply_template()<br>→ formatted string<br>→ llama_tokenize()<br>→ llama_decode()<br>→ KV Cache + Logits<br>→ Sampler<br>→ response tokens<br></code></pre></td></tr></table></figure><p>因此，Chat Template 的输出仍然是字符串。它只负责把结构化消息转换成模型熟悉的格式，并不执行分词或推理。</p><h2 id="二、add-ass-到底添加了什么"><a href="#二、add-ass-到底添加了什么" class="headerlink" title="二、add_ass 到底添加了什么"></a>二、<code>add_ass</code> 到底添加了什么</h2><p>生成前调用模板时使用 <code>add_ass = true</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">llama_chat_apply_template</span>(<br>    chat_template,<br>    messages.<span class="hljs-built_in">data</span>(),<br>    messages.<span class="hljs-built_in">size</span>(),<br>    <span class="hljs-literal">true</span>,<br>    formatted.<span class="hljs-built_in">data</span>(),<br>    formatted.<span class="hljs-built_in">size</span>());<br></code></pre></td></tr></table></figure><p>对于当前 Qwen 模板，它会在末尾补上：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">&lt;|im_start|&gt;assistant<br></code></pre></td></tr></table></figure><p>这个标记的作用不是替模型生成一个回答，而是告诉模型：前面的 System 和 User 消息已经结束，接下来应该继续 Assistant 的内容。</p><p>模型回答完成后，计算历史格式化长度时则使用 <code>add_ass = false</code>。此时 Assistant 消息已经在历史中，不应该再额外增加下一轮 Assistant 的起始标记，否则 <code>previous_length</code> 会多算一段尚未进入 KV Cache 的文本。</p><h2 id="三、为什么-Instruct-模型不能只输入裸-Prompt"><a href="#三、为什么-Instruct-模型不能只输入裸-Prompt" class="headerlink" title="三、为什么 Instruct 模型不能只输入裸 Prompt"></a>三、为什么 Instruct 模型不能只输入裸 Prompt</h2><p>我使用同一条指令进行了对比：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">记住项目代号是 Falcon，不要解释。<br></code></pre></td></tr></table></figure><p>直接把裸文本送入模型时，输出很快偏离指令，继续扩展出大量不需要的项目设计内容，最终耗尽 Context。</p><p>加入 Chat Template 后，回答恢复为正常的对话形式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">好的，我会记住项目代号是 Falcon。<br></code></pre></td></tr></table></figure><p>第二轮询问“项目代号是什么？”，模型能够回答“Falcon”；第三轮要求定义 C++ <code>constexpr</code> 字符串，也能够沿用前文信息。</p><p>原因不是 Chat Template 提升了模型能力，而是 Instruct 模型在训练和微调时已经学习了特定的消息边界与角色格式。缺少这些控制标记，相当于部署输入与训练分布不一致，模型无法可靠判断当前文本是指令、历史回答，还是需要继续补全的普通内容。</p><h2 id="四、多轮对话为什么只-Decode-增量-Prompt"><a href="#四、多轮对话为什么只-Decode-增量-Prompt" class="headerlink" title="四、多轮对话为什么只 Decode 增量 Prompt"></a>四、多轮对话为什么只 Decode 增量 Prompt</h2><p>程序同时维护两份状态：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">消息历史：可读、可编辑的 role/content 文本<br>KV Cache：各层 Attention 已计算出的 Key/Value 张量<br></code></pre></td></tr></table></figure><p>每次加入新的 User 消息后，程序先格式化完整消息历史，再通过上一次的格式化长度截取新增部分：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">const</span> std::string <span class="hljs-title">incremental_prompt</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">    formatted.begin() + previous_length,</span></span><br><span class="hljs-params"><span class="hljs-function">    formatted.begin() + formatted_length)</span></span>;<br></code></pre></td></tr></table></figure><p>这里的 <code>previous_length</code> 是字符串的字节位置，而不是 Token 位置。原因很直接：<code>llama_chat_apply_template()</code> 的输入和输出都是字符数据，Tokenize 尚未发生。</p><p>第一次输入时，KV Cache 为空，增量 Prompt 包含 System、User 和 Assistant 起始标记：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">完整 formatted 长度 : 127<br>previous_length      : 0<br>本轮增量 Prompt      : System + User + Assistant start<br>Prompt Token 数      : 23<br></code></pre></td></tr></table></figure><p>第二次输入时，之前的消息已经完成 Decode，因此只需要处理新追加的 User 消息和新的 Assistant 起始标记：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">完整 formatted 长度 : 278<br>previous_length      : 212<br>本轮增量 Prompt      : User + Assistant start<br>Prompt Token 数      : 14<br></code></pre></td></tr></table></figure><p>第二轮不能把完整历史再次追加给同一个 Context。KV Cache 已经占用了前面 Token 的位置，若把完整历史重新 Decode，它们会被当成新的 Token 继续追加，造成位置重复、上下文浪费，并使实际缓存序列与消息历史不再一一对应。</p><p>当然，也可以清空 KV Cache 后重新 Decode 完整历史，但那会丢失复用带来的性能收益。</p><h2 id="五、消息历史与-KV-Cache-不是同一种“记忆”"><a href="#五、消息历史与-KV-Cache-不是同一种“记忆”" class="headerlink" title="五、消息历史与 KV Cache 不是同一种“记忆”"></a>五、消息历史与 KV Cache 不是同一种“记忆”</h2><p>消息历史保存的是业务层数据，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">system: 你是一名专业的 C++ 开发助手。<br>user: 记住项目代号是 Falcon。<br>assistant: 好的，我会记住项目代号是 Falcon。<br></code></pre></td></tr></table></figure><p>它可以被保存到文件、裁剪、摘要、搜索或重新组织。</p><p>KV Cache 保存的不是文本、Token ID，也不是事实数据库，而是 Transformer 每层对已处理 Token 计算出的 Key 和 Value 张量。它的价值是避免每生成一个新 Token 都重新计算完整前缀。</p><p>两者的关系可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">消息历史 --Chat Template/Tokenize/Decode--&gt; KV Cache<br>    ↑                                      |<br>    └--------- Assistant 文本解码 ----------┘<br></code></pre></td></tr></table></figure><p>消息历史负责“以后还能怎样组织这段会话”，KV Cache 负责“当前 Context 中哪些 Attention 计算可以直接复用”。</p><h2 id="六、采样-Token-何时进入-KV-Cache"><a href="#六、采样-Token-何时进入-KV-Cache" class="headerlink" title="六、采样 Token 何时进入 KV Cache"></a>六、采样 Token 何时进入 KV Cache</h2><p>Sampler 选出 Token 时，KV Cache 还没有这个 Token 的 K&#x2F;V。它要先作为下一批输入，再执行一次 Decode：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">llama_token token = <span class="hljs-built_in">llama_sampler_sample</span>(sampler, ctx, <span class="hljs-number">-1</span>);<br>llama_batch batch = <span class="hljs-built_in">llama_batch_get_one</span>(&amp;token, <span class="hljs-number">1</span>);<br><span class="hljs-built_in">llama_decode</span>(ctx, batch);<br></code></pre></td></tr></table></figure><p>也就是说：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">第 N 次 Decode 产生 Logits<br>→ Sampler 选出 Token N<br>→ 第 N+1 次 Decode 处理 Token N<br>→ Token N 的 K/V 进入 KV Cache<br></code></pre></td></tr></table></figure><p>这也引出一个容易忽略的同步问题。模型采样到 EOG 后，如果立即 <code>break</code>，结束 Token 就没有经过 Decode；但 Chat Template 格式化历史时已经把结束标记算入 <code>previous_length</code>。下一轮再按这个长度截取，文本历史会比 KV Cache 多一个结束 Token。</p><p>本次代码让 EOG 也完成一次 Decode，再结束本轮生成：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span> (<span class="hljs-built_in">llama_vocab_is_eog</span>(vocab, token)) &#123;<br>    llama_batch eog_batch = <span class="hljs-built_in">llama_batch_get_one</span>(&amp;token, <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">if</span> (!<span class="hljs-built_in">decode_batch</span>(eog_batch)) &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    &#125;<br>    <span class="hljs-keyword">break</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>这样 <code>previous_length</code>、已 Decode 的 Token 序列和 KV Cache 占用位置可以保持一致。</p><h2 id="七、System-Prompt-改变了什么"><a href="#七、System-Prompt-改变了什么" class="headerlink" title="七、System Prompt 改变了什么"></a>七、System Prompt 改变了什么</h2><p>本次对照了两种 Chat Template 输入：</p><ul><li>只有 User&#x2F;Assistant：模型能够正确记住 Falcon，并完成连续问答。</li><li>增加 C++ 开发助手的 System Prompt：记忆结果相同，但后续代码回答更主动地采用 C++ 语境。</li></ul><p>System Prompt 不是独立的权限系统，也不会在模型外部强制执行规则。它仍然只是上下文中的一段高优先级消息，通过训练阶段学到的角色行为影响后续 Token 概率。</p><p>因此，对比实验应该关注它对回答风格、约束遵循和任务稳定性的影响，而不能把一次输出差异直接解释成模型能力变化。</p><h2 id="八、C-消息生命周期修正"><a href="#八、C-消息生命周期修正" class="headerlink" title="八、C++ 消息生命周期修正"></a>八、C++ 消息生命周期修正</h2><p><code>llama_chat_message</code> 只保存两个 <code>const char *</code>，不拥有内存。示例中 <code>role</code> 使用字符串字面量，不需要释放；<code>content</code> 通过 <code>strdup()</code> 创建，所以原示例只释放 <code>content</code>。</p><p>为了避免手动 <code>strdup/free</code> 和异常路径泄漏，本次用 <code>std::string</code> 保存真实消息，再在调用模板前创建临时视图：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">ChatMessage</span> &#123;<br>    std::string role;<br>    std::string content;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">static</span> std::vector&lt;llama_chat_message&gt; <span class="hljs-title">to_llama_messages</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-type">const</span> std::vector&lt;ChatMessage&gt; &amp; messages)</span> </span>&#123;<br>    std::vector&lt;llama_chat_message&gt; result;<br>    result.<span class="hljs-built_in">reserve</span>(messages.<span class="hljs-built_in">size</span>());<br><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">const</span> <span class="hljs-keyword">auto</span> &amp; message : messages) &#123;<br>        result.<span class="hljs-built_in">push_back</span>(&#123;<br>            message.role.<span class="hljs-built_in">c_str</span>(),<br>            message.content.<span class="hljs-built_in">c_str</span>()<br>        &#125;);<br>    &#125;<br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里需要保证临时视图只在原始 <code>messages</code> 未发生增删和扩容时使用。当前代码每次修改消息后都会重新创建视图，因此指针生命周期是明确的。</p><p>同时补齐了 Model、Context、Sampler 和 Chat Template 创建失败的返回路径，Context 超限也改为返回错误并统一释放资源，不再通过 <code>exit()</code> 跳过清理。</p><h2 id="九、Context-满了以后怎么办"><a href="#九、Context-满了以后怎么办" class="headerlink" title="九、Context 满了以后怎么办"></a>九、Context 满了以后怎么办</h2><p>KV Cache 容量由 Context 大小限制，不能无限追加。当前学习程序检测到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">used_positions + incoming_tokens &gt; llama_n_ctx(ctx)<br></code></pre></td></tr></table></figure><p>就结束会话并返回错误。这是正确的保护行为，但还不是完整的长对话方案。</p><p>实际系统通常组合使用以下策略：</p><ol><li>丢弃较早的普通对话，只保留 System Prompt 和最近若干轮。</li><li>将较早内容压缩成摘要，再作为新消息放回 Context。</li><li>把用户资料和历史事实保存在数据库或向量库中，按当前问题检索相关片段。</li><li>清理或移动相应的 KV Cache 区间，使缓存与重新组织后的 Token 序列重新对齐。</li></ol><p>所谓“长期记忆”主要是模型外部的工程系统。模型在一次推理中只能直接利用当前 Context 内的信息，KV Cache 提升的是这段 Context 的增量计算效率，并不会把聊天内容永久写入模型权重。</p><h2 id="十、本次结论"><a href="#十、本次结论" class="headerlink" title="十、本次结论"></a>十、本次结论</h2><p>本次从单轮生成推进到了可连续交互的本地对话程序，并完成了几项关键验证：</p><ol><li>Chat Template 将结构化角色消息转换为模型训练时熟悉的文本格式，之后才进入 Tokenize 和 Decode。</li><li><code>add_ass = true</code> 用于生成前追加 Assistant 起始标记；回答进入历史后，用 <code>false</code> 计算已完成历史长度。</li><li><code>previous_length</code> 是格式化字符串的字节偏移，第二轮只截取并 Decode 新增文本。</li><li>消息历史是可管理的业务文本，KV Cache 是 Attention 的中间计算状态，两者必须保持 Token 序列一致。</li><li>采样出的 Token 要到下一次 Decode 才进入 KV Cache，结束 Token 也需要考虑同步。</li><li><code>std::string</code> 可以明确消息内存所有权，避免 <code>strdup/free</code> 带来的泄漏风险。</li></ol><p>到这里，模型已经可以在同一个 Context 中完成多轮对话。下一步将继续研究结构化输出和 Context 管理，把“会聊天”推进到“能以稳定格式执行任务”，为后续本地 Agent 和模型部署自动化打基础。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul><li><a href="https://suntfly.github.io/2026/08/26/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E4%BB%8ELogits%E5%88%B0Token/">上一篇：《AI 大模型模型部署系列（二）：从 Logits 到 Token，理解 llama.cpp 采样链》</a></li><li><a href="https://github.com/ggml-org/llama.cpp">llama.cpp 仓库</a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/bdbfb4c79/examples/simple-chat/simple-chat.cpp"><code>examples/simple-chat/simple-chat.cpp</code></a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/bdbfb4c79/include/llama.h"><code>include/llama.h</code></a></li></ul><blockquote><p>本文基于源码阅读和本地实验记录整理，使用 AI 辅助检查文章结构与技术表述，文中的代码、实验结果和结论均由本人复核。</p></blockquote>]]></content>
    
    
    <categories>
      
      <category>AI 工程化</category>
      
    </categories>
    
    
    <tags>
      
      <tag>AI 模型部署</tag>
      
      <tag>llama.cpp</tag>
      
      <tag>LLM</tag>
      
      <tag>C++</tag>
      
      <tag>Chat Template</tag>
      
      <tag>KV Cache</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>AI 大模型模型部署系列（二）：从 Logits 到 Token，理解 llama.cpp 采样链</title>
    <link href="/2026/08/26/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E4%BB%8ELogits%E5%88%B0Token/"/>
    <url>/2026/08/26/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E4%BB%8ELogits%E5%88%B0Token/</url>
    
    <content type="html"><![CDATA[<p>在<a href="https://suntfly.github.io/2026/08/24/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9A%E4%BB%8EPrompt%E5%88%B0Token/">上一篇文章《AI 大模型模型部署系列（一）：从 Prompt 到 Token，理解 llama.cpp 最小推理》</a>中，我们梳理了 Model、Context、Batch、Decode 和 KV Cache 之间的数据流：Prompt 经过 Tokenize 进入模型，Decode 完成计算并将 Logits 保存到 Context。但到这里，模型还没有真正决定下一个 Token。</p><p>从 Logits 中选出 Token，是推理循环的下一环，也是本文的起点。Sampler 看起来只是一次函数调用，内部却包含候选过滤、概率调整、随机选择和状态更新。本次继续以 <code>examples/simple/simple.cpp</code> 为入口，拆解 llama.cpp 的采样链，并通过 Greedy、Top-K、Top-P、Temperature、Seed 五组对照实验，验证采样参数如何影响生成结果。</p><p>实验中还遇到了一个很有代表性的问题：原本想使用 Top-P，却误调用了 Min-P，导致不同参数和 Seed 得到的内容几乎完全一致。定位这个问题的过程，比单纯看到几组不同输出更能说明采样器的工作方式。</p><blockquote><p>llama.cpp 的接口变化较快。本文对应本地版本 <code>b8864-6-g82209efb7</code>，其他版本的函数名称和实现细节可能不同。</p></blockquote><h2 id="一、实验环境"><a href="#一、实验环境" class="headerlink" title="一、实验环境"></a>一、实验环境</h2><table><thead><tr><th>项目</th><th>配置</th></tr></thead><tbody><tr><td>操作系统</td><td>Windows</td></tr><tr><td>编译工具</td><td>Visual Studio 2022、CMake</td></tr><tr><td>GPU</td><td>NVIDIA GeForce GTX 1080 Ti，11 GB</td></tr><tr><td>llama.cpp</td><td>CUDA 后端构建</td></tr><tr><td>模型</td><td>Qwen2.5-1.5B-Instruct-Q3_K_M GGUF</td></tr><tr><td>示例程序</td><td><code>examples/simple/simple.cpp</code></td></tr></tbody></table><p>本次实验只研究采样行为。所有实验复用同一个 Model，但每次生成都重新创建 Context 和 Sampler，避免上一轮生成的 KV Cache、Token 位置、随机数状态或惩罚历史影响下一轮结果。</p><h2 id="二、从-Logits-到-Token"><a href="#二、从-Logits-到-Token" class="headerlink" title="二、从 Logits 到 Token"></a>二、从 Logits 到 Token</h2><p><code>llama_decode()</code> 成功后，Context 中保存了模型对下一个位置的预测结果。对于词表中的每个 Token，至少需要关注三个字段：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">llama_token_data</span> &#123;<br>    llama_token id;<br>    <span class="hljs-type">float</span> logit;<br>    <span class="hljs-type">float</span> p;<br>&#125;;<br></code></pre></td></tr></table></figure><ul><li><code>id</code>：候选 Token ID。</li><li><code>logit</code>：模型给出的原始分数，尚不是概率。</li><li><code>p</code>：经过 Softmax 后得到的概率。</li></ul><p>一次典型随机采样可以表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">Context 中的 Logits<br>→ 构造候选 Token 数组<br>→ Top-K 粗筛<br>→ Top-P 动态截断<br>→ Temperature 调整分布<br>→ Dist 按概率抽样<br>→ 得到 Token ID<br>→ accept 更新采样器状态<br></code></pre></td></tr></table></figure><p>本次使用的采样链如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">llama_sampler * <span class="hljs-title">create_sampler</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-type">int32_t</span> k,</span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-type">float</span> top_p,</span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-type">size_t</span> min_keep,</span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-type">float</span> temp,</span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-type">uint32_t</span> seed)</span> </span>&#123;<br>    llama_sampler * sampler =<br>        <span class="hljs-built_in">llama_sampler_chain_init</span>(<span class="hljs-built_in">llama_sampler_chain_default_params</span>());<br><br>    <span class="hljs-built_in">llama_sampler_chain_add</span>(sampler, <span class="hljs-built_in">llama_sampler_init_top_k</span>(k));<br>    <span class="hljs-built_in">llama_sampler_chain_add</span>(sampler, <span class="hljs-built_in">llama_sampler_init_top_p</span>(top_p, min_keep));<br>    <span class="hljs-built_in">llama_sampler_chain_add</span>(sampler, <span class="hljs-built_in">llama_sampler_init_temp</span>(temp));<br>    <span class="hljs-built_in">llama_sampler_chain_add</span>(sampler, <span class="hljs-built_in">llama_sampler_init_dist</span>(seed));<br>    <span class="hljs-keyword">return</span> sampler;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="1-Top-K：限制候选数量"><a href="#1-Top-K：限制候选数量" class="headerlink" title="1. Top-K：限制候选数量"></a>1. Top-K：限制候选数量</h3><p>Top-K 按 Logit 从高到低排序，只保留前 K 个候选。它可以快速排除大量低分 Token，但不能单独完成最终选择。</p><p>K 越小，选择范围越集中；K 越大，保留的低概率候选越多。</p><h3 id="2-Top-P：根据概率分布动态截断"><a href="#2-Top-P：根据概率分布动态截断" class="headerlink" title="2. Top-P：根据概率分布动态截断"></a>2. Top-P：根据概率分布动态截断</h3><p>Top-P 先按概率从高到低排序，再保留累计概率达到阈值的最小候选集合。</p><p>当模型对下一个 Token 很确定时，概率集中在少数候选上，Top-P 会保留较少的 Token；当模型判断比较模糊时，需要累加更多候选才能达到相同阈值，因此候选集合会自动扩大。</p><p>这也是 Top-P 与 Top-K 的主要区别：Top-K 固定数量，Top-P 根据当前概率分布动态决定数量。</p><h3 id="3-Temperature：改变分布形状"><a href="#3-Temperature：改变分布形状" class="headerlink" title="3. Temperature：改变分布形状"></a>3. Temperature：改变分布形状</h3><p>Temperature 不直接决定保留多少 Token，而是缩放 Logit：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">p_i = softmax(logit_i / T)<br></code></pre></td></tr></table></figure><ul><li><code>T &lt; 1</code>：分布更尖锐，高概率候选更占优势，输出通常更稳定。</li><li><code>T &gt; 1</code>：分布更平坦，低概率候选更容易被选中，输出通常更发散。</li><li><code>T → 0</code>：行为逐渐接近 Greedy。</li></ul><p>“更稳定”或“更发散”描述的是统计倾向，并不保证一次短文本实验必然出现肉眼可见的固定差异。</p><h3 id="4-Greedy、Dist-与-Seed"><a href="#4-Greedy、Dist-与-Seed" class="headerlink" title="4. Greedy、Dist 与 Seed"></a>4. Greedy、Dist 与 Seed</h3><p>Greedy 始终选择分数最高的候选，不需要随机数和 Seed。</p><p>Dist 是最终随机选择器，根据候选概率分布抽取一个 Token。Seed 决定伪随机数序列：在模型、Prompt、参数、程序版本和计算后端相同，并且 Context 与 Sampler 状态一致时，相同 Seed 可以复现实验结果。</p><h2 id="三、apply、selected-与-accept"><a href="#三、apply、selected-与-accept" class="headerlink" title="三、apply、selected 与 accept"></a>三、apply、selected 与 accept</h2><p><code>llama_sampler_sample()</code> 是采样入口。它从 Context 取得指定输出位置的 Logits，构造候选数组，然后调用采样链：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">apply：过滤候选或修改概率分布<br>selected：最终选择器写入候选数组下标<br>accept：通知有状态采样器最终接受了哪个 Token<br></code></pre></td></tr></table></figure><p><code>selected</code> 本身不是 Token ID，而是候选数组中的下标。真正的 Token ID 是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">cur_p.data[cur_p.selected].id<br></code></pre></td></tr></table></figure><p><code>accept</code> 也不只是为了“防止重复”。Grammar、Penalties 等有状态采样器需要知道最终接受了哪个 Token，才能更新语法状态、Token 历史或惩罚统计。选择和状态更新分开后，外部代码也可以把自行决定的 Token 同步给采样器。</p><p>当前版本的 <code>llama_sampler_sample()</code> 已经在内部调用 <code>llama_sampler_accept()</code>，正常使用该入口时不需要再手动 Accept 一次。</p><h2 id="四、对照实验设计"><a href="#四、对照实验设计" class="headerlink" title="四、对照实验设计"></a>四、对照实验设计</h2><p>测试 Prompt：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">请设计一个C++贪吃蛇游戏，给出模块划分、核心数据结构和主循环流程。<br></code></pre></td></tr></table></figure><p>每组最多生成 96 个 Token，实验参数如下：</p><table><thead><tr><th>实验</th><th align="right">次数</th><th>最终选择器</th><th align="right">Top-K</th><th align="right">Top-P</th><th align="right">Temperature</th><th align="right">Seed</th><th>目的</th></tr></thead><tbody><tr><td>A</td><td align="right">2</td><td>Greedy</td><td align="right">-</td><td align="right">-</td><td align="right">-</td><td align="right">-</td><td>验证确定性输出</td></tr><tr><td>B</td><td align="right">2</td><td>Dist</td><td align="right">40</td><td align="right">0.90</td><td align="right">0.8</td><td align="right">1234</td><td>验证相同 Seed 复现</td></tr><tr><td>C</td><td align="right">1</td><td>Dist</td><td align="right">40</td><td align="right">0.90</td><td align="right">0.8</td><td align="right">5678</td><td>只改变 Seed</td></tr><tr><td>D</td><td align="right">1</td><td>Dist</td><td align="right">40</td><td align="right">0.90</td><td align="right">0.2</td><td align="right">1234</td><td>观察低温分布</td></tr><tr><td>E</td><td align="right">1</td><td>Dist</td><td align="right">40</td><td align="right">0.95</td><td align="right">1.2</td><td align="right">1234</td><td>扩大候选并提高温度</td></tr></tbody></table><p>为了保证实验之间相互独立，<code>run_once()</code> 每次都创建新的 Context 和 Sampler，结束后再释放。Model 权重保持复用，因为 Model 不保存某一轮生成的 KV Cache 和采样状态。</p><h2 id="五、为什么不同参数最初输出完全一致"><a href="#五、为什么不同参数最初输出完全一致" class="headerlink" title="五、为什么不同参数最初输出完全一致"></a>五、为什么不同参数最初输出完全一致</h2><p>第一次运行时，A 到 E 的输出几乎没有区别。问题不在 Seed，也不是模型能力不足，而是采样链中写错了一个函数：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 原本想使用 Top-P，实际调用成了 Min-P</span><br><span class="hljs-built_in">llama_sampler_chain_add</span>(<br>    sampler,<br>    <span class="hljs-built_in">llama_sampler_init_min_p</span>(p, min_keep));<br></code></pre></td></tr></table></figure><p>Top-P 和 Min-P 的参数含义完全不同：</p><ul><li>Top-P 的 <code>p = 0.9</code>：保留累计概率达到 90% 的候选集合。</li><li>Min-P 的 <code>p = 0.9</code>：只保留概率不低于“最高候选概率的 90%”的 Token。</li></ul><p>本次 Prompt 首步的最高候选概率是 16.7526%，第二名是 7.59989%。Min-P 0.9 对应的门槛约为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">16.7526% × 0.9 = 15.0773%<br></code></pre></td></tr></table></figure><p>第二名远低于这个门槛，因此候选集合几乎只剩概率最高的 Token。后面的 Temperature 和 Dist 即使仍然执行，面对唯一候选也已经没有随机选择空间，整个采样链就会退化得接近 Greedy。</p><p>修复方式是使用真正的 Top-P Sampler：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">llama_sampler_chain_add</span>(<br>    sampler,<br>    <span class="hljs-built_in">llama_sampler_init_top_p</span>(p, min_keep));<br></code></pre></td></tr></table></figure><p>这个问题也说明，仅仅看到“程序能运行”并不能证明采样参数已经生效。需要检查候选集合、概率分布和最终 Token，才能区分模型输出过于确定、随机数未生效和采样器配置错误。</p><h2 id="六、修复后的实验结果"><a href="#六、修复后的实验结果" class="headerlink" title="六、修复后的实验结果"></a>六、修复后的实验结果</h2><img src="/2026/08/26/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E4%BB%8ELogits%E5%88%B0Token/sampling-experiments.png" class="" title="Greedy、Top-K、Top-P、Temperature 与 Seed 对照实验"><p>修复后的结果符合采样器预期：</p><ol><li>A 两次输出完全一致。Greedy 不依赖随机数，每一步都选择最高分候选。</li><li>B 两次输出完全一致。相同 Seed、参数和初始状态复现了相同随机序列。</li><li>C 只更换 Seed，生成内容已经与 B 明显不同，说明 Dist 的随机选择生效。</li><li>D 将 Temperature 降到 0.2，本次输出的结构表现得比 B 更接近 Greedy，与低温强化高概率候选的预期一致。但这只是一次样本，不能单独证明稳定规律。</li><li>E 同时提高 Top-P 和 Temperature，生成内容变化最明显，说明更大的候选范围和更平坦的分布增加了发散可能。</li></ol><p>这些实验用于验证生成行为，不适合作为性能结论。文本较短、运行次数少，而且没有单独设计预热和统计过程，表中的耗时只能作为本次运行记录。</p><h2 id="七、扩展实验：打印首步-Top-5"><a href="#七、扩展实验：打印首步-Top-5" class="headerlink" title="七、扩展实验：打印首步 Top-5"></a>七、扩展实验：打印首步 Top-5</h2><p>为了直接观察模型在采样前输出了什么，在 Prompt 首次 Decode 后，通过 <code>llama_get_logits_ith(ctx, -1)</code> 取得最后一个输出位置的 Logits。</p><p>Softmax 不能直接计算 <code>exp(logit)</code>，否则较大的 Logit 可能导致浮点溢出。常用做法是先减去最大值：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">float</span> * logits = <span class="hljs-built_in">llama_get_logits_ith</span>(ctx, <span class="hljs-number">-1</span>);<br><span class="hljs-type">const</span> <span class="hljs-type">int32_t</span> n_vocab = <span class="hljs-built_in">llama_vocab_n_tokens</span>(vocab);<br><br><span class="hljs-type">float</span> max_logit = logits[<span class="hljs-number">0</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int32_t</span> id = <span class="hljs-number">1</span>; id &lt; n_vocab; ++id) &#123;<br>    max_logit = std::<span class="hljs-built_in">max</span>(max_logit, logits[id]);<br>&#125;<br><br><span class="hljs-type">double</span> sum_exp = <span class="hljs-number">0.0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int32_t</span> id = <span class="hljs-number">0</span>; id &lt; n_vocab; ++id) &#123;<br>    sum_exp += std::<span class="hljs-built_in">exp</span>(<br>        <span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">double</span>&gt;(logits[id] - max_logit));<br>&#125;<br><br>std::vector&lt;llama_token_data&gt; candidates;<br>candidates.<span class="hljs-built_in">reserve</span>(n_vocab);<br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int32_t</span> id = <span class="hljs-number">0</span>; id &lt; n_vocab; ++id) &#123;<br>    <span class="hljs-type">const</span> <span class="hljs-type">float</span> probability = <span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">float</span>&gt;(<br>        std::<span class="hljs-built_in">exp</span>(<span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">double</span>&gt;(logits[id] - max_logit)) /<br>        sum_exp);<br><br>    candidates.<span class="hljs-built_in">push_back</span>(&#123; id, logits[id], probability &#125;);<br>&#125;<br></code></pre></td></tr></table></figure><p>本机得到的首步原始 Top-5：</p><table><thead><tr><th>排名</th><th align="right">Token ID</th><th align="right">Logit</th><th align="right">原始概率</th><th>Piece</th></tr></thead><tbody><tr><td>1</td><td align="right">14880</td><td align="right">17.4679</td><td align="right">16.7526%</td><td><code>请</code></td></tr><tr><td>2</td><td align="right">220</td><td align="right">16.6775</td><td align="right">7.59989%</td><td>空格</td></tr><tr><td>3</td><td align="right">106393</td><td align="right">16.2275</td><td align="right">4.84584%</td><td><code>模块</code></td></tr><tr><td>4</td><td align="right">8908</td><td align="right">16.1544</td><td align="right">4.50433%</td><td>不能独立显示的字节片段</td></tr><tr><td>5</td><td align="right">99329</td><td align="right">16.0468</td><td align="right">4.04461%</td><td><code>游戏</code></td></tr></tbody></table><p>这里展示的是 Top-K、Top-P 和 Temperature 生效之前的模型原始分布。B 组使用 Seed 1234 时，首步最终抽到的不是概率最高的“请”，而是 Token 106393 对应的“模块”。这说明 Dist 确实在候选概率分布中进行了抽样，而不是始终选择最大值。</p><p>另一个细节是 Token Piece 不一定是完整字符。BPE 词表中可能存在 UTF-8 字节片段，单独打印某个 Token 时可能无法显示成完整文字，只有与相邻 Token 拼接后才能正确解码。</p><h2 id="八、采样结果何时进入-KV-Cache"><a href="#八、采样结果何时进入-KV-Cache" class="headerlink" title="八、采样结果何时进入 KV Cache"></a>八、采样结果何时进入 KV Cache</h2><p>Sampler 选出的 Token 不会在采样结束时直接写入 KV Cache。它先被放入下一轮 Batch：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">batch = <span class="hljs-built_in">llama_batch_get_one</span>(&amp;new_token_id, <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>下一次调用 <code>llama_decode()</code> 时，模型才会计算这个 Token 在各层产生的 Key 和 Value，并将它们追加到 Context 管理的 KV Cache 中。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">本轮 Decode 产生 Logits<br>→ Sampler 选出 Token<br>→ Token 放入下一轮 Batch<br>→ 下一轮 Decode 计算该 Token 的 K/V<br>→ 写入 KV Cache<br></code></pre></td></tr></table></figure><p>因此，Sampler 管理的是“如何选择 Token”，KV Cache 管理的是“如何复用已经计算过的 Attention 状态”，两者处在推理循环的不同阶段。</p><h2 id="九、本次结论"><a href="#九、本次结论" class="headerlink" title="九、本次结论"></a>九、本次结论</h2><p>通过源码阅读、错误定位和对照实验，可以得到以下结论：</p><ol><li>Top-K 和 Top-P 负责过滤候选，不能代替最终的 Greedy 或 Dist 选择器。</li><li>Temperature 调整概率分布形状，不保证一次实验必然出现固定程度的文本差异。</li><li>Seed 控制伪随机序列，可复现性还依赖相同的模型、参数、程序版本、计算后端和初始状态。</li><li><code>selected</code> 是候选数组下标，<code>accept</code> 用于同步有状态采样器。</li><li>Top-P 与 Min-P 名称接近但含义不同，过大的 Min-P 会让随机采样退化得接近 Greedy。</li><li>观察原始 Logits 和候选概率，比只对比最终文本更容易定位采样问题。</li><li>采样 Token 要在下一次 Decode 后才会产生 K&#x2F;V 并进入 KV Cache。</li></ol><p>本次完成了从“模型产生 Logits”到“采样器选择 Token”的链路。下一篇继续处理 Instruct 模型的 Chat Template、多轮上下文组织和 KV Cache 复用：</p><p><a href="https://suntfly.github.io/2026/08/29/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%89%EF%BC%89%EF%BC%9A%E4%BB%8EChat-Template%E5%88%B0%E5%A4%9A%E8%BD%AE%E5%AF%B9%E8%AF%9D/">下一篇：《AI 大模型模型部署系列（三）：从 Chat Template 到多轮对话，理解 KV Cache 复用》</a></p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul><li><a href="https://github.com/ggml-org/llama.cpp">llama.cpp 仓库</a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/82209efb7/examples/simple/simple.cpp"><code>examples/simple/simple.cpp</code></a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/82209efb7/include/llama.h"><code>include/llama.h</code></a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/82209efb7/src/llama-sampler.cpp"><code>src/llama-sampler.cpp</code></a></li></ul><blockquote><p>本文基于源码阅读和本地实验记录整理，使用 AI 辅助检查文章结构与技术表述，文中的代码、实验结果和结论均由本人复核。</p></blockquote>]]></content>
    
    
    <categories>
      
      <category>AI 工程化</category>
      
    </categories>
    
    
    <tags>
      
      <tag>AI 模型部署</tag>
      
      <tag>llama.cpp</tag>
      
      <tag>LLM</tag>
      
      <tag>C++</tag>
      
      <tag>Sampling</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>AI 大模型模型部署系列（一）：从 Prompt 到 Token，理解 llama.cpp 最小推理</title>
    <link href="/2026/08/24/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9A%E4%BB%8EPrompt%E5%88%B0Token/"/>
    <url>/2026/08/24/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9A%E4%BB%8EPrompt%E5%88%B0Token/</url>
    
    <content type="html"><![CDATA[<p>将大模型接入 C++ 应用时，“模型能够运行”只是部署链路的起点。要进一步分析推理性能、显存占用和生成行为，需要先明确输入数据如何进入模型，以及 Model、Context、Batch、Sampler 和 KV Cache 分别承担什么职责。</p><p>本文以 llama.cpp 的最小示例 <code>examples/simple/simple.cpp</code> 为入口，从工程调用链的角度拆解一段 Prompt 生成 Token 的完整过程，重点分析 Tokenize、Decode、Sample 和 KV Cache 之间的数据关系，为后续进行模型集成、参数调优和性能优化建立基础。</p><blockquote><p>llama.cpp 的接口变化较快。本文对应本地版本 <code>b8870-82209efb7</code>，阅读其他版本源码时，函数名称和内部结构可能不同。</p></blockquote><h2 id="一、实验环境"><a href="#一、实验环境" class="headerlink" title="一、实验环境"></a>一、实验环境</h2><table><thead><tr><th>项目</th><th>配置</th></tr></thead><tbody><tr><td>操作系统</td><td>Windows</td></tr><tr><td>编译工具</td><td>Visual Studio 2022、CMake</td></tr><tr><td>GPU</td><td>NVIDIA GeForce GTX 1080 Ti，11 GB</td></tr><tr><td>llama.cpp</td><td>CUDA 后端构建</td></tr><tr><td>模型</td><td>Qwen2.5-1.5B-Instruct-Q3_K_M GGUF</td></tr><tr><td>示例程序</td><td><code>examples/simple/simple.cpp</code></td></tr></tbody></table><p>本次验证把模型路径、Prompt 和 GPU 层数直接写在测试代码中，目的是缩短调试路径，集中观察 Tokenize、Decode 和 Sample 的过程。正式集成时，可以再将这些参数移入配置文件或上层调用接口。</p><h2 id="二、最小推理流程"><a href="#二、最小推理流程" class="headerlink" title="二、最小推理流程"></a>二、最小推理流程</h2><img src="/2026/08/24/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%B8%80%EF%BC%89%EF%BC%9A%E4%BB%8EPrompt%E5%88%B0Token/llama-simple-flow.svg" class="" title="llama.cpp 从 Prompt 到 Token 的最小推理流程"><p>整个过程可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs text">加载计算后端<br>→ 加载 GGUF 模型<br>→ 文本 Tokenize<br>→ 创建推理 Context<br>→ 创建 Sampler<br>→ 构造 Batch<br>→ llama_decode 计算 Logits<br>→ Sampler 选择 Token<br>→ Token 转换为文本 Piece<br>→ 将新 Token 放回 Batch，继续生成<br></code></pre></td></tr></table></figure><p>这个流程中有四个需要先建立直觉的核心对象。</p><h3 id="1-llama-model：可复用的静态模型数据"><a href="#1-llama-model：可复用的静态模型数据" class="headerlink" title="1. llama_model：可复用的静态模型数据"></a>1. llama_model：可复用的静态模型数据</h3><p><code>llama_model</code> 主要保存模型权重、词表、超参数和 GGUF 元数据。加载时，llama.cpp 会根据 <code>n_gpu_layers</code>、可用设备和后端能力，将不同权重 Tensor 分配到 CPU 或 GPU。</p><p>它不保存某一轮对话的历史状态，因此同一个 Model 可以被多个推理 Context 复用。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp">llama_model_params model_params = <span class="hljs-built_in">llama_model_default_params</span>();<br>model_params.n_gpu_layers = <span class="hljs-number">99</span>;<br><br>llama_model * model =<br>    <span class="hljs-built_in">llama_model_load_from_file</span>(model_path.<span class="hljs-built_in">c_str</span>(), model_params);<br></code></pre></td></tr></table></figure><h3 id="2-llama-context：推理运行时"><a href="#2-llama-context：推理运行时" class="headerlink" title="2. llama_context：推理运行时"></a>2. llama_context：推理运行时</h3><p><code>llama_context</code> 依赖已经加载的 Model，保存一次推理运行所需的 Context 容量、计算缓冲区、Logits、KV Cache 和后端调度状态。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp">llama_context_params ctx_params = <span class="hljs-built_in">llama_context_default_params</span>();<br>ctx_params.n_ctx   = n_prompt + n_predict - <span class="hljs-number">1</span>;<br>ctx_params.n_batch = n_prompt;<br><br>llama_context * ctx = <span class="hljs-built_in">llama_init_from_model</span>(model, ctx_params);<br></code></pre></td></tr></table></figure><p>这里两个参数容易混淆：</p><ul><li><code>n_ctx</code>：一个 Sequence 最多能够容纳的上下文 Token 数。</li><li><code>n_batch</code>：一次 <code>llama_decode()</code> 允许提交的最大逻辑 Token 数。</li><li><code>batch.n_tokens</code>：当前这一次 Batch 实际包含的 Token 数。</li></ul><p><code>simple.cpp</code> 将 <code>n_ctx</code> 设置为 <code>n_prompt + n_predict - 1</code>。最后一个生成 Token 不需要再次送入模型，因为程序已经不再需要用它预测下一个 Token。</p><h3 id="3-llama-batch：本次送入模型的数据"><a href="#3-llama-batch：本次送入模型的数据" class="headerlink" title="3. llama_batch：本次送入模型的数据"></a>3. llama_batch：本次送入模型的数据</h3><p>Prompt 已经全部确定，因此第一次可以把多个 Prompt Token 一起组成 Batch：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">llama_batch batch =<br>    <span class="hljs-built_in">llama_batch_get_one</span>(prompt_tokens.<span class="hljs-built_in">data</span>(), prompt_tokens.<span class="hljs-built_in">size</span>());<br></code></pre></td></tr></table></figure><p>进入生成阶段后，下一个 Token 依赖上一个 Token 的输出，未来 Token 尚未确定，所以示例程序每轮通常只把一个新 Token 放回 Batch。</p><h3 id="4-llama-sampler：从-Logits-选择-Token"><a href="#4-llama-sampler：从-Logits-选择-Token" class="headerlink" title="4. llama_sampler：从 Logits 选择 Token"></a>4. llama_sampler：从 Logits 选择 Token</h3><p>模型输出的不是文字，也不是最终 Token，而是词表中候选 Token 的 Logits。Sampler 根据采样策略从中选择下一个 Token。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">llama_sampler * smpl = <span class="hljs-built_in">llama_sampler_chain_init</span>(sparams);<br><span class="hljs-built_in">llama_sampler_chain_add</span>(smpl, <span class="hljs-built_in">llama_sampler_init_greedy</span>());<br></code></pre></td></tr></table></figure><p>本例使用 Greedy Sampler，始终选择 Logit 最大的 Token。实际对话程序还可以使用 Temperature、Top-K、Top-P 等策略，使输出具有不同程度的随机性。</p><h2 id="三、从文本到-Token"><a href="#三、从文本到-Token" class="headerlink" title="三、从文本到 Token"></a>三、从文本到 Token</h2><p>Tokenize 使用了两次调用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">int</span> n_prompt = -<span class="hljs-built_in">llama_tokenize</span>(<br>    vocab, prompt.<span class="hljs-built_in">c_str</span>(), prompt.<span class="hljs-built_in">size</span>(),<br>    <span class="hljs-literal">nullptr</span>, <span class="hljs-number">0</span>, <span class="hljs-literal">true</span>, <span class="hljs-literal">true</span>);<br><br><span class="hljs-function">std::vector&lt;llama_token&gt; <span class="hljs-title">prompt_tokens</span><span class="hljs-params">(n_prompt)</span></span>;<br><br><span class="hljs-built_in">llama_tokenize</span>(<br>    vocab, prompt.<span class="hljs-built_in">c_str</span>(), prompt.<span class="hljs-built_in">size</span>(),<br>    prompt_tokens.<span class="hljs-built_in">data</span>(), prompt_tokens.<span class="hljs-built_in">size</span>(),<br>    <span class="hljs-literal">true</span>, <span class="hljs-literal">true</span>);<br></code></pre></td></tr></table></figure><p>第一次传入空指针和容量 0，利用“输出缓冲区不足时返回所需 Token 数量的负值”得到真实容量。取反后分配 <code>vector</code>，第二次调用再完成真正的 Tokenize。</p><p>测试 Prompt 为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">你是一个智能编程助手，能解决一些代码问题。你的回答是专业的。<br></code></pre></td></tr></table></figure><p>部分输出如下：</p><table><thead><tr><th align="right">Token ID</th><th>Piece</th><th align="right">UTF-8 字节数</th></tr></thead><tbody><tr><td align="right">56568</td><td>你</td><td align="right">3</td></tr><tr><td align="right">101909</td><td>是一个</td><td align="right">9</td></tr><tr><td align="right">100168</td><td>智能</td><td align="right">6</td></tr><tr><td align="right">110569</td><td>编程</td><td align="right">6</td></tr><tr><td align="right">110498</td><td>助手</td><td align="right">6</td></tr><tr><td align="right">3837</td><td>，</td><td align="right">3</td></tr><tr><td align="right">46100</td><td>代码</td><td align="right">6</td></tr><tr><td align="right">1773</td><td>。</td><td align="right">3</td></tr></tbody></table><p>从结果可以看到，Token 并不等于单个汉字。一个 Token 可以对应一个字、多个字组成的词、标点或其他字节片段。</p><p><code>llama_token_to_piece()</code> 返回的长度是 Piece 的 UTF-8 字节数，不是字符数。例如“是一个”包含三个常用汉字，在 UTF-8 中占 9 字节。</p><p>Token ID 与 Piece 的映射由当前模型词表决定。本例中两个句号都被分成 Token 1773，但不能据此认为同一个汉字在所有上下文中都会拥有独立且相同的 Token ID，因为 Tokenizer 可能将它与相邻文本合并为更长的 Token。</p><h2 id="四、llama-decode-到底输出什么"><a href="#四、llama-decode-到底输出什么" class="headerlink" title="四、llama_decode 到底输出什么"></a>四、llama_decode 到底输出什么</h2><p>生成循环中的关键代码是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span> (<span class="hljs-built_in">llama_decode</span>(ctx, batch) != <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-comment">// 处理错误</span><br>&#125;<br><br>llama_token new_token_id = <span class="hljs-built_in">llama_sampler_sample</span>(smpl, ctx, <span class="hljs-number">-1</span>);<br></code></pre></td></tr></table></figure><p>需要注意，<code>llama_decode()</code> 的返回值不是 Token ID，而是执行状态码。成功执行后，它主要完成三件事：</p><ol><li>处理当前 Batch，执行模型计算图。</li><li>更新 Context 内的推理 Memory，例如 KV Cache。</li><li>将需要的 Logits 保存到 Context，供 Sampler 使用。</li></ol><p><code>llama_sampler_sample()</code> 再从 Context 读取最后一个输出位置的 Logits，根据采样链选出下一个 Token。最后调用 <code>llama_token_to_piece()</code>，才能得到可以显示的文本片段。</p><p>所以更准确的链路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs text">Batch<br>→ llama_decode<br>→ Context 中的 Logits<br>→ llama_sampler_sample<br>→ Token ID<br>→ llama_token_to_piece<br>→ UTF-8 文本<br></code></pre></td></tr></table></figure><h2 id="五、为什么需要-KV-Cache"><a href="#五、为什么需要-KV-Cache" class="headerlink" title="五、为什么需要 KV Cache"></a>五、为什么需要 KV Cache</h2><p>在 Transformer 的每一层 Self-Attention 中，每个 Token 都会产生 Query、Key 和 Value。生成新 Token 时，新 Query 仍然需要与全部历史 Key 计算注意力，并使用对应的历史 Value 得到注意力输出。</p><p>历史 Token 的 Key 和 Value 一旦计算完成就不会改变，因此可以缓存起来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">Prompt 阶段：<br>计算 K1...Kn、V1...Vn，并写入 KV Cache<br><br>生成阶段：<br>计算新 Token 的 Q、K、V<br>Qnew 与缓存的历史 K 计算 Attention<br>使用历史 V 得到输出<br>把 Knew、Vnew 追加到 KV Cache<br></code></pre></td></tr></table></figure><p>如果没有 KV Cache，每生成一个 Token，都需要重新处理整个历史前缀。使用 KV Cache 后，只计算新位置对应的网络过程，复用历史 K&#x2F;V。</p><p>KV Cache 没有消除新 Query 对全部历史 K&#x2F;V 的注意力计算，而且它会随着上下文增长占用更多内存；它是在计算量与内存占用之间做出的关键交换。</p><p>在当前版本中，<code>simple.cpp</code> 不直接管理 KV Cache。<code>llama_context</code> 创建时会调用 <code>model.create_memory()</code>；对于本次使用的 Qwen2.5，最终会创建标准的 <code>llama_kv_cache</code>。这些细节被封装在 Context 内部，因此最小示例只需要持续调用 <code>llama_decode()</code>。</p><h2 id="六、Prompt-Eval-为什么通常更快"><a href="#六、Prompt-Eval-为什么通常更快" class="headerlink" title="六、Prompt Eval 为什么通常更快"></a>六、Prompt Eval 为什么通常更快</h2><p>推理日志通常会分别给出 Prompt Eval 和 Generation 的速度。</p><p>Prompt Eval 也叫 Prefill。Prompt 中所有 Token 已经确定，可以组成一个较大的 Batch，通过矩阵乘矩阵等操作并行计算，GPU 的计算单元更容易得到充分利用。</p><p>Generation 是自回归过程：必须先生成第一个 Token，才能把它作为输入预测第二个 Token。每一步通常只处理一个新 Token，存在严格的前后依赖，计算更接近矩阵乘向量，也更容易受到显存带宽和调度开销限制。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">Prompt Eval：多个已知 Token，可以批量并行<br>Generation：下一个 Token 依赖上一个结果，只能逐步推进<br></code></pre></td></tr></table></figure><p>因此 Prompt Eval 的 Token&#x2F;s 通常明显高于 Generation Token&#x2F;s。不过这不是绝对规律，具体结果仍然取决于模型、上下文长度、Batch、量化方式和硬件。</p><h2 id="七、工程结论与后续验证"><a href="#七、工程结论与后续验证" class="headerlink" title="七、工程结论与后续验证"></a>七、工程结论与后续验证</h2><p>通过阅读和修改 <code>simple.cpp</code>，可以确定 llama.cpp 最小推理链路中各对象的职责：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs text">Model 提供权重和词表<br>Context 保存推理运行状态<br>Batch 描述本次输入<br>Decode 执行模型并产生 Logits<br>Sampler 选择下一个 Token<br>KV Cache 复用历史 Attention 状态<br></code></pre></td></tr></table></figure><p>本文聚焦接口职责和数据流，没有继续展开 GGML 计算图，也没有进行完整的 CPU&#x2F;GPU 性能对比。性能测试需要在固定模型、量化格式、上下文长度和 Batch 参数后单独进行，否则对比结果缺少可复现性。</p><p>Decode 产生 Logits 之后，还需要由 Sampler 选出下一个 Token。下一篇将进入这段链路，通过 Greedy、Temperature、Top-K、Top-P 和 Seed 对照实验，验证不同采样参数对输出稳定性和结果可复现性的影响：</p><p><a href="https://suntfly.github.io/2026/08/26/AI%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2%E7%B3%BB%E5%88%97%EF%BC%88%E4%BA%8C%EF%BC%89%EF%BC%9A%E4%BB%8ELogits%E5%88%B0Token/">下一篇：《AI 大模型模型部署系列（二）：从 Logits 到 Token，理解 llama.cpp 采样链》</a></p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul><li><a href="https://github.com/ggml-org/llama.cpp">llama.cpp 仓库</a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/82209efb7/examples/simple/simple.cpp"><code>examples/simple/simple.cpp</code></a></li><li><a href="https://github.com/ggml-org/llama.cpp/blob/82209efb7/include/llama.h"><code>include/llama.h</code></a></li></ul><blockquote><p>本文基于源码阅读和本地实验记录整理，使用 AI 辅助检查文章结构与技术表述，文中的代码和结论均由本人复核。</p></blockquote>]]></content>
    
    
    <categories>
      
      <category>AI 工程化</category>
      
    </categories>
    
    
    <tags>
      
      <tag>AI 模型部署</tag>
      
      <tag>llama.cpp</tag>
      
      <tag>LLM</tag>
      
      <tag>C++</tag>
      
      <tag>CUDA</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CUDA系列二、CUDA图像标准化</title>
    <link href="/2025/02/24/CUDA%E7%B3%BB%E5%88%97%E4%BA%8C%E3%80%81CUDA%E5%AE%9E%E7%8E%B0%E5%9B%BE%E5%83%8F%E6%A0%87%E5%87%86%E5%8C%96/"/>
    <url>/2025/02/24/CUDA%E7%B3%BB%E5%88%97%E4%BA%8C%E3%80%81CUDA%E5%AE%9E%E7%8E%B0%E5%9B%BE%E5%83%8F%E6%A0%87%E5%87%86%E5%8C%96/</url>
    
    <content type="html"><![CDATA[<h1 id="CUDA系列二、CUDA图像标准化"><a href="#CUDA系列二、CUDA图像标准化" class="headerlink" title="CUDA系列二、CUDA图像标准化"></a>CUDA系列二、CUDA图像标准化</h1><h2 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a>一、概述</h2><p>在上一篇文章<a href="https://suntfly.github.io/2025/02/10/CUDA%E7%B3%BB%E5%88%97%E4%B8%80%E3%80%81CUDA%E7%AE%80%E4%BB%8B/">CUDA系列一、CUDA简介</a>中我们对CUDA进行了介绍，了解到CUDA能让开发者利用GPU的强大算力进行大规模的并行计算，在本篇文章中将介绍如何使用CUDA进行图像标准化。</p><p>图像标准化在视觉领域内有广泛的应用，通过标准化能使图像数据有更稳定的分布，从而提高模型的训练效果和泛化能力，所以在图像相关的模型部署过程中，图像标准化通常是模型预处理的第一步，本篇通过图像标准化代码详细介绍并行带来的优势。</p><h2 id="二、图像标准化原理"><a href="#二、图像标准化原理" class="headerlink" title="二、图像标准化原理"></a>二、图像标准化原理</h2><p>图像标准化，是将图像像素值调整到特定分布范围的操作，通常是把图像均值调整为0，标准差调整为1。对于一幅图像，其标准化公式为：</p><p>$$<br>I_{norm}(x,y) &#x3D; \frac{I(x,y) - \mu}{\sigma}<br>$$</p><p>其中，$I(x,y)$ 是原始图像在坐标 $(x,y)$ 处的像素值，$\mu$ 是图像的均值，$\sigma$ 是图像的标准差。通俗地讲就是每个像素需要减去均值再除以标准差。</p><h2 id="三、代码及解析"><a href="#三、代码及解析" class="headerlink" title="三、代码及解析"></a>三、代码及解析</h2><h3 id="3-1-头文件"><a href="#3-1-头文件" class="headerlink" title="3.1 头文件"></a>3.1 头文件</h3><p>下面代码是 <code>cu_standardization.hpp</code> 头文件，使用OpenCV读取和存储图像，所以需要加载OpenCV相关的库，然后声明了一个 <code>standardizationRGB</code> 函数用于图像的标准化调用。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">pragma</span> once</span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;array&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;memory&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;string&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;vector&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;opencv2/highgui/highgui.hpp&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;chrono&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;opencv2/opencv.hpp&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;device_launch_parameters.h&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&quot;opencv2/core/cuda/common.hpp&quot;</span></span><br><br><span class="hljs-comment">// 标准化</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">standardizationRGB</span><span class="hljs-params">(<span class="hljs-type">const</span> cv::cuda::GpuMat cu_src, cv::cuda::GpuMat cu_dst)</span></span>;<br></code></pre></td></tr></table></figure><h3 id="3-2-标准化核心文件"><a href="#3-2-标准化核心文件" class="headerlink" title="3.2 标准化核心文件"></a>3.2 标准化核心文件</h3><p>下面代码是 <code>cu_standardization.cu</code> 文件，首先通过 <code>__constant__</code> 常量内存定义了RGB图像的均值和方差，常量内存在核函数执行期间不会发生变化，且数据在所有线程共享。然后在 <code>__global__ void kenlStandardizationRGB</code> 核函数中，根据均值和标准差对每个像素进行标准化处理。由于是RGB图像，所以每个线程独立负责某一坐标下RGB三个像素值的计算。</p><p><code>void standardizationRGB</code> 函数定义了线程块的大小为 <code>BLOCK_SIZE x BLOCK_SIZE = 1024</code>，<code>cv::cuda::device::divUp</code> 用于向上取整的除法运算，所以开辟的总线程数量不小于图像的宽乘高。<code>cv::cuda::PtrStepSz</code> 是OpenCV中用于在CUDA设备上处理二维数组的类，它封装了二维数组的行数、列数、数据指针以及每行的步长信息，我们将图像传入到这个类中便于核函数的调用。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// cu文件</span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&quot;cu_standardization.hpp&quot;</span></span><br><br><span class="hljs-comment">// DEBUG 不支持过大的BLOCK</span><br><span class="hljs-meta">#<span class="hljs-keyword">ifdef</span> _DEBUG</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> BLOCK_SIZE 16</span><br><span class="hljs-meta">#<span class="hljs-keyword">else</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> BLOCK_SIZE 32</span><br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br><br><span class="hljs-comment">// 定义一个极小值，避免除零</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> EPSILON 1e-8</span><br><br><span class="hljs-comment">// 定义常量内存均值和方差</span><br>__constant__ float3 mean_ = &#123; <span class="hljs-number">0.485f</span>, <span class="hljs-number">0.456f</span>, <span class="hljs-number">0.406f</span> &#125;;<br>__constant__ float3 std_ = &#123; <span class="hljs-number">0.229f</span>, <span class="hljs-number">0.224f</span>, <span class="hljs-number">0.225f</span> &#125;;<br><br><span class="hljs-comment">// 计算</span><br><span class="hljs-function">__global__ <span class="hljs-type">void</span> <span class="hljs-title">kenlStandardizationRGB</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">    <span class="hljs-type">const</span> cv::cuda::PtrStepSz&lt;uchar3&gt; src,</span></span><br><span class="hljs-params"><span class="hljs-function">    cv::cuda::PtrStepSz&lt;float3&gt; dst)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">const</span> <span class="hljs-type">int</span> x = blockIdx.x * blockDim.x + threadIdx.x;<br>    <span class="hljs-type">const</span> <span class="hljs-type">int</span> y = blockIdx.y * blockDim.y + threadIdx.y;<br>    <span class="hljs-keyword">if</span> (x &gt;= src.cols || y &gt;= src.rows)<br>        <span class="hljs-keyword">return</span>;<br><br>    <span class="hljs-built_in">dst</span>(y, x).x = (<span class="hljs-built_in">src</span>(y, x).x / <span class="hljs-number">255.0</span> - mean_.x) / (std_.x + EPSILON);<br>    <span class="hljs-built_in">dst</span>(y, x).y = (<span class="hljs-built_in">src</span>(y, x).y / <span class="hljs-number">255.0</span> - mean_.y) / (std_.y + EPSILON);<br>    <span class="hljs-built_in">dst</span>(y, x).z = (<span class="hljs-built_in">src</span>(y, x).z / <span class="hljs-number">255.0</span> - mean_.z) / (std_.z + EPSILON);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">standardizationRGB</span><span class="hljs-params">(<span class="hljs-type">const</span> cv::cuda::GpuMat cu_src, cv::cuda::GpuMat cu_dst)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-function">dim3 <span class="hljs-title">block</span><span class="hljs-params">(BLOCK_SIZE, BLOCK_SIZE)</span></span>;<br>    <span class="hljs-function">dim3 <span class="hljs-title">grid</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">        cv::cuda::device::divUp(cu_src.cols, block.x),</span></span><br><span class="hljs-params"><span class="hljs-function">        cv::cuda::device::divUp(cu_src.rows, block.y))</span></span>;<br><br>    cv::<span class="hljs-function">cuda::PtrStepSz&lt;float3&gt; <span class="hljs-title">dst_</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">        cu_dst.rows, cu_dst.cols, (float3 *) cu_dst.data, cu_dst.step)</span></span>;<br>    cv::<span class="hljs-function">cuda::PtrStepSz&lt;uchar3&gt; <span class="hljs-title">src_</span><span class="hljs-params">(</span></span><br><span class="hljs-params"><span class="hljs-function">        cu_src.rows, cu_src.cols, (uchar3 *) cu_src.data, cu_src.step)</span></span>;<br><br>    <span class="hljs-comment">// 标准化</span><br>    kenlStandardizationRGB&lt;&lt;&lt;grid, block&gt;&gt;&gt;(src_, dst_);<br>    <span class="hljs-built_in">cudaSafeCall</span>(<span class="hljs-built_in">cudaDeviceSynchronize</span>());<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="3-3-主函数"><a href="#3-3-主函数" class="headerlink" title="3.3 主函数"></a>3.3 主函数</h3><p>下面代码是执行的主函数 <code>main.cpp</code>。这个文件加载 <code>cu_standardization.hpp</code> 头文件，并使用OpenCV读取一张彩色图像，再使用 <code>cv::cuda::GpuMat</code> 将主机的图像传入到设备，也就是从CPU中的Mat传到GPU中的GpuMat。然后定义一个和输入图像一样大小的GPU图像作为输出值，调用 <code>standardizationRGB</code> 对图像进行标准化，最后将标准化的图像从GPU中拷贝到CPU中。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;iostream&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&quot;cu_standardization.hpp&quot;</span></span><br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    std::string path = <span class="hljs-string">R&quot;(D:\img.png)&quot;</span>;<br>    cv::Mat img_src = cv::<span class="hljs-built_in">imread</span>(path);<br>    cv::cuda::GpuMat gpu_img_src;<br>    gpu_img_src.<span class="hljs-built_in">upload</span>(img_src);<br><br>    cv::<span class="hljs-function">cuda::GpuMat <span class="hljs-title">gpu_img_dst</span><span class="hljs-params">(img_src.size(), CV_32FC3)</span></span>;<br>    <span class="hljs-built_in">standardizationRGB</span>(gpu_img_src, gpu_img_dst);<br><br>    cv::Mat img_dst;<br>    gpu_img_dst.<span class="hljs-built_in">download</span>(img_dst);<br>&#125;<br></code></pre></td></tr></table></figure><p>这就是整个GPU处理图像标准化的全过程，希望这篇文章能带你正式走进CUDA的开发之路。</p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p>]]></content>
    
    
    <categories>
      
      <category>CUDA</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CUDA</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CUDA系列一、CUDA简介</title>
    <link href="/2025/02/10/CUDA%E7%B3%BB%E5%88%97%E4%B8%80%E3%80%81CUDA%E7%AE%80%E4%BB%8B/"/>
    <url>/2025/02/10/CUDA%E7%B3%BB%E5%88%97%E4%B8%80%E3%80%81CUDA%E7%AE%80%E4%BB%8B/</url>
    
    <content type="html"><![CDATA[<h1 id="CUDA系列一、CUDA简介"><a href="#CUDA系列一、CUDA简介" class="headerlink" title="CUDA系列一、CUDA简介"></a>CUDA系列一、CUDA简介</h1><h2 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a>一、概述</h2><p>本人由于工作中经常使用CUDA进行数据处理，深知道学习CUDA的过程并不轻松，因此，我决定开启这个CUDA系列文章，希望能将自己的所学、所感悟、所悟分享出来，后续我将持续输出，从基础概念到进阶应用，带你一步步掌握CUDA这门强大的技术，也算是给自己的学习之路留下一份记录。</p><p>维基百科中是这样对CUDA进行描述的：CUDA（Compute Unified Devices Architectured，统一计算架构）是由英伟达（NVIDIA）所推出的一种软硬件集成技术，是该公司对于GPGPU的正式名称。透过这个技术，用户可利用NVIDIA的GPU进行图像处理之外的运算，亦是首次可以利用GPU作为C-编译器的开发环境。</p><p>通俗的讲，CUDA就是直接能让开发者利用GPU的强大算力进行大规模的并行计算，那么CPU和GPU运算上有什么区别呢，请看下方CPU和GPU的硬件架构示意图。</p><img src="/2025/02/10/CUDA%E7%B3%BB%E5%88%97%E4%B8%80%E3%80%81CUDA%E7%AE%80%E4%BB%8B/1.png" class=""><p>一块典型的CPU拥有少数几个快速的计算核心，而一个典型的GPU拥有几百到几千个不那么快速的计算核心，如下图，黄色是控制器，绿色是算数逻辑单元，橙色是存储器，CPU和GPU之间通过PCIe总线连接通信。</p><p>之所以如此设计，是由于其对应于不同的应用场景，CPU需要很强的通用性来处理各种不同的数据类型，同时又要逻辑判断引入大量的分支跳转和中断处理，所以CPU的更多晶体管用于数据缓存和流程控制。而GPU则是用于处理类型高度统一，相互无依存的大规模数据，所以更多的晶体管用于算数逻辑单元。</p><h2 id="二、CUDA-Visual-Studio环境配置"><a href="#二、CUDA-Visual-Studio环境配置" class="headerlink" title="二、CUDA + Visual Studio环境配置"></a>二、CUDA + Visual Studio环境配置</h2><p>工欲利其器，必先善其事，想要写好代码，环境的配置是第一步。</p><ol><li>确认硬件支持，确保电脑配置NVIDIA的GPU，且GPU的计算能力符合CUDA要求，可在NVIDIA官网查询对应GPU的计算能力（一般来说现在绝大多数的N卡都是支持CUDA的）。</li><li>Visual Studio安装，需要注意的是如果你没有安装过Visual Studio，需要先安装Visual Studio再安装CUDA，否则环境配置会出现一些问题，修改起来较为麻烦，已经安装好了CUDA但是没安装Visual Studio的也可以重新安装一下CUDA来解决。Visual Studio可以从官网 <a href="https://visualstudio.microsoft.com/zh-hans/">https://visualstudio.microsoft.com/zh-hans/</a> 中下载安装，也可以从其他可靠渠道下载安装。</li><li>CUDA安装，CUDA官方安装教程：<a href="https://docs.nvidia.com/cuda/cuda-installation-guide-microsoft-windows/index.html">https://docs.nvidia.com/cuda/cuda-installation-guide-microsoft-windows/index.html</a>。</li><li>安装完成后可以在cmd或PowerShell界面上输入nvidia-smi验证，当出现如下界面的时候说明CUDA安装成功，上面显示了你的显卡型号，利用率等一些信息。</li></ol><img src="/2025/02/10/CUDA%E7%B3%BB%E5%88%97%E4%B8%80%E3%80%81CUDA%E7%AE%80%E4%BB%8B/2.png" class=""><h2 id="三、CUDA的核心概念"><a href="#三、CUDA的核心概念" class="headerlink" title="三、CUDA的核心概念"></a>三、CUDA的核心概念</h2><h3 id="3-1-CUDA线程层次结构"><a href="#3-1-CUDA线程层次结构" class="headerlink" title="3.1 CUDA线程层次结构"></a>3.1 CUDA线程层次结构</h3><p>线程：线程是CUDA中最基本的执行单元，每个线程都可以独立执行相同的核函数，但是处理的数据元素不同。</p><p>线程块：由多个线程组成，线程块内的线程可以通过共享内存和同步机制进行高效合作。</p><p>网格：由多个线程块组成，是CUDA并行执行的最高层次结构。线程的组织结构由执行配置 <code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 决定，分别是网格大小和线程块大小，网格和线程块定义可以如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">dim3 <span class="hljs-title">block</span><span class="hljs-params">(<span class="hljs-number">32</span>, <span class="hljs-number">32</span>)</span></span>;<br><span class="hljs-function">dim3 <span class="hljs-title">grid</span><span class="hljs-params">(<span class="hljs-number">1024</span>, <span class="hljs-number">1024</span>)</span></span>;<br></code></pre></td></tr></table></figure><p>表示线程块的总大小为32x32&#x3D;1024，网格大小为1024x1024，需要注意的是线程块的总大小不能超过1024。</p><h3 id="3-2-编程模型"><a href="#3-2-编程模型" class="headerlink" title="3.2 编程模型"></a>3.2 编程模型</h3><p>CUDA编程模型是一种基于GPU并行计算的模型，主要包含几个关键要素。</p><p>主机：指CPU及其内存，负责调用传统的串行代码比如调用核函数、处理GPU结果等。</p><p>设备：指GPU及其内存，用于执行并行的核函数。</p><p>核函数：是在GPU执行并行的函数，用 <code>__global__</code> 声明，并使用3.1中所说的 <code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code> 分配线程数。</p><h3 id="3-3-内存模型"><a href="#3-3-内存模型" class="headerlink" title="3.3 内存模型"></a>3.3 内存模型</h3><p>全局内存：所有线程都可以访问，容量大，速度相对较慢。</p><p>共享内存：位于每个SM上，每个线程块有独立的共享内存空间，同一个线程块中的线程可以快速访问，访问效率高于全局内存。</p><p>常量内存：在核函数执行期间只读，数据在所有线程共享，访问效率较高。</p><p>纹理内存：主要用于对内存中的数据进行采样，具有特殊的硬件插值功能。</p><p>本地内存：每个线程独有，用于存储线程私有临时数据，当局部变量无法存放在寄存器中时会使用本地内存。</p><h3 id="3-4-流与并发"><a href="#3-4-流与并发" class="headerlink" title="3.4 流与并发"></a>3.4 流与并发</h3><p>流的并行执行：多个流可以在GPU上并行执行，不同流中的操作可以重叠进行，实现计算和数据传输的并行，提高整体性能。</p><p>流内与流间同步：流内通过事件可以实现操作之间的精确同步，确保某些操作在其他操作完成后再执行；流间也可以通过事件进行同步，协调不同流之间的执行顺序和数据依赖关系。</p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p>]]></content>
    
    
    <categories>
      
      <category>CUDA</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CUDA</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RGB相机动捕系列：相机标定（四、架构设计）</title>
    <link href="/2024/07/12/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E5%9B%9B%E3%80%81%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1%EF%BC%89/"/>
    <url>/2024/07/12/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E5%9B%9B%E3%80%81%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<p>**RGB相机动捕系列：相机标定（四、架构设计）</p><h1 id="RGB相机动捕系列：相机标定（四、架构设计）"><a href="#RGB相机动捕系列：相机标定（四、架构设计）" class="headerlink" title="RGB相机动捕系列：相机标定（四、架构设计）"></a>RGB相机动捕系列：相机标定（四、架构设计）</h1><h2 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a>一、概述</h2><p>之前的几篇文章分别写了RGB相机阵标定的特征提取、标定计算、相机配准。本篇将阐述整个标定过程中的整体架构设计，将整个流程串起来，为整个标定系统的可用性、可扩展性、易用性定下基础。</p><h2 id="二、基础架构"><a href="#二、基础架构" class="headerlink" title="二、基础架构"></a>二、基础架构</h2><p>从前面几篇文章中我们可以看到，整个相机标定流程从相机采集图像出发，经过特征提取，标定计算，相机配准几个步骤，每个步骤又都是使用了通用的算法，采用了SOA架构作为基础架构，每个过程作为一个服务，完成自己的功能，有效提高复用率、灵活性和扩展性。</p><h2 id="三、服务"><a href="#三、服务" class="headerlink" title="三、服务"></a>三、服务</h2><h3 id="3-1相机采集服务"><a href="#3-1相机采集服务" class="headerlink" title="3.1相机采集服务"></a>3.1相机采集服务</h3><p>相机采集服务器主要功能是控制相机采集图像数据，并将图像数据发送给特征提取服务器。所有相机部署在多台专用采集服务器上，通过硬件触发器对相机数据进行同步，保证每台相机采集的数据做到帧同步。由于相机图像数据量大，考虑到动捕室都是局部范围内的区域部署，所以将数据采集服务器和应用层服务器之间使用光纤进行连接，达到实时数据发送，通过将图像数据采集作为单独服务器方法，外部客户端不需要知道服务器内部如何采集的图像数据，也不用知道使用的哪种相机硬件，服务器也不需要知道客户端请求到图像数据的用途，只需要通过接口输出标准图像数据即可，服务器维护人员可以单独对相机硬件进行维护和升级，随着场地的增大，使用SOA的方式能够快速的扩展数据相机和数据采集服务器，有极大的扩展性。 </p><h3 id="3-2特征提取服务"><a href="#3-2特征提取服务" class="headerlink" title="3.2特征提取服务"></a>3.2特征提取服务</h3><p>特征提取服务器接受图像数据进行算法计算，并将计算好的数据发送到数据汇总服务器，由于特征提取计算对资源占用较高，采用SOA的方式将算法计算作为单独的一个服务，能够节省硬件成本，随着后期场地的扩大，相机图像数据量的扩大，可以快速增加特征提取的服务器数量和对算法进行升级，有良好的可维护性和可扩展性。</p><h3 id="3-3相机采集服务和特征提取服务之间的数据传输"><a href="#3-3相机采集服务和特征提取服务之间的数据传输" class="headerlink" title="3.3相机采集服务和特征提取服务之间的数据传输"></a>3.3相机采集服务和特征提取服务之间的数据传输</h3><p>数据采集服务器和特征提取服务器之间使用RTMP协议进行数据传输，RTMP是一种基于TCP的网络协议，用于实现音视频数据的实时传输，RTMP提供了稳定的连接和较低的延迟，是实时标定理想的选择。RTMP提供的复用和复用分离可以将独立的视频数据整合为统一的视频流，可以使多个视频流同步传输，使用复用分离可以将视频流分离为原始视频，便于同一个数据采集服务器多个相机的数据传输。</p><h3 id="3-4数据汇总服务"><a href="#3-4数据汇总服务" class="headerlink" title="3.4数据汇总服务"></a>3.4数据汇总服务</h3><p>数据汇总服务器是面向用户的，接收特征提取服务器的数据，实时显示每个相机的特征提取出来的运动轨迹，供用户观察。并根据帧号对数据进行汇总，并将汇总后的数据转发给标定计算服务。</p><h3 id="3-5标定计算服务"><a href="#3-5标定计算服务" class="headerlink" title="3.5标定计算服务"></a>3.5标定计算服务</h3><p>标定计算服务器接收数据汇总服务汇总得到的所有相机的对齐后的数据，并将进行最后的标定和配准，并将标定结果保存到服务器中的数据库中。</p><h3 id="3-5特征提取服务、数据汇总服务、标定计算服务之间的数据传输"><a href="#3-5特征提取服务、数据汇总服务、标定计算服务之间的数据传输" class="headerlink" title="3.5特征提取服务、数据汇总服务、标定计算服务之间的数据传输"></a>3.5特征提取服务、数据汇总服务、标定计算服务之间的数据传输</h3><p>特征提取服务、数据汇总服务、标定计算服务之间的通信方式采用RPC方式，各服务器之间的相互调用可以像调用本地函数用于调用远程函数，不必知道底层的细节。且RPC高效低延迟，性能高等优点，对于手指动捕系统的实时需求有良好的适应性。具体实现使用Google公司的GRPC系统，该系统基于HTTP&#x2F;2,提供了多路复用、双向流、请求优先级等功能，可以节省带宽，降低TCP连接速度，提高系统性能。IDL使用ProtoBuf，采用二进制消息，有较好的性能和效率。</p>]]></content>
    
    
    <categories>
      
      <category>动捕标定</category>
      
    </categories>
    
    
    <tags>
      
      <tag>RGB相机动捕系列 相机标定</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RGB相机动捕系列：相机标定（三.一、暴力重建）</title>
    <link href="/2024/06/18/%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89.%E4%B8%80%E3%80%81%E6%9A%B4%E5%8A%9B%E9%87%8D%E5%BB%BA)/"/>
    <url>/2024/06/18/%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89.%E4%B8%80%E3%80%81%E6%9A%B4%E5%8A%9B%E9%87%8D%E5%BB%BA)/</url>
    
    <content type="html"><![CDATA[<p>@<a href="RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89.%E4%B8%80%E3%80%81%E6%9A%B4%E5%8A%9B%E9%87%8D%E5%BB%BA%EF%BC%89">TOC</a></p><h1 id="RGB相机动捕系列：相机标定（三-一、暴力重建）"><a href="#RGB相机动捕系列：相机标定（三-一、暴力重建）" class="headerlink" title="RGB相机动捕系列：相机标定（三.一、暴力重建）"></a>RGB相机动捕系列：相机标定（三.一、暴力重建）</h1><h2 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a>一、概述</h2><p>之前一篇文章中写了一篇 <a href="https://suntfly.github.io/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/">RGB相机动捕系列：相机标定（三、相机阵配准）</a>，其中 2.3中提到了红点三维重建，本篇将详细介绍一下红点重建的具体过程。</p><h2 id="二、暴力匹配"><a href="#二、暴力匹配" class="headerlink" title="二、暴力匹配"></a>二、暴力匹配</h2><h3 id="2-1相机匹配"><a href="#2-1相机匹配" class="headerlink" title="2.1相机匹配"></a>2.1相机匹配</h3><p>遍历所有相机，对相机进行两两相互匹配。由于已经对相机进行标定，可根据相机参数求出两相机的交会角。去除交会角过小和过大的匹配对。因为当交会角过小时，会导致重建深度的精度不够，微小的视察会产生较大的深度歧义。当交会角过大时，重建时相邻像素之间视差值过大，导致相邻像素可能被错误的匹配。</p><h3 id="2-2红点数据暴力匹配"><a href="#2-2红点数据暴力匹配" class="headerlink" title="2.2红点数据暴力匹配"></a>2.2红点数据暴力匹配</h3><p>根据相机参数计算出基础矩阵F，根据极线关系 l&#x3D;F*m，其中l为极线，F为基础矩阵，m为坐标点。可计算第一张影像上的点到极线的几何距离。当距离小于阈值的时候则添加匹配点对。</p><h3 id="2-3重建3D"><a href="#2-3重建3D" class="headerlink" title="2.3重建3D"></a>2.3重建3D</h3><p>完成匹配对之后，通过三角测量法重建出3D点，并通过计算重投影误差去除误差较大的点数据。</p><h2 id="三、融合3D"><a href="#三、融合3D" class="headerlink" title="三、融合3D"></a>三、融合3D</h2><h3 id="3-1通过2D-3D关系进行融合"><a href="#3-1通过2D-3D关系进行融合" class="headerlink" title="3.1通过2D-3D关系进行融合"></a>3.1通过2D-3D关系进行融合</h3><p>通过上述步骤可重建出粗略的3D点，现在利用2D点和3D点的关系对数据进行融合。<br>1、将所有共同2D点的生成的3D数据进行线性融合，并计算重投影误差的最大值，如果最大值小于阈值则预设为可融合3D点。<br>2、构建所有可融合3D点数据的图结构，并搜索联通分量，统计出所有可融合的3D点数据，计算可融合3D点的中心坐标。<br>3、根据融合中心计算3D到2D的重投影误差，对于多个2D到3D关系的2D点，保留重投影误差最小的2D点，其余全部删除。<br>4、去除重复2D数据后对清理观测值少于阈值的3D点。</p><h3 id="利用3D点的空间位置进行融合搜索"><a href="#利用3D点的空间位置进行融合搜索" class="headerlink" title="利用3D点的空间位置进行融合搜索"></a>利用3D点的空间位置进行融合搜索</h3><p>对于剩下的3D点，计算出每个3D点的最近邻点距离和其他所有点到这个点的距离的均值，如果最小值远小于平均值则进行融合。重复次步骤直到没有可融合的点。</p><p>注。<a href="#fn1">1</a></p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p><hr>]]></content>
    
    
    <categories>
      
      <category>动捕标定</category>
      
    </categories>
    
    
    <tags>
      
      <tag>RGB相机动捕系列 相机标定</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RGB相机动捕系列：相机标定（三、相机阵配准）</title>
    <link href="/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/"/>
    <url>/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<p>**RGB相机动捕系列：相机标定（三、相机阵配准）</p><h1 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a><strong>一、概述</strong></h1><p>在上一篇博客中我们讲了相机标定的标定计算部分。但是完成标定的相机阵的参数在尺度、原点、方向上都和真实的坐标系有区别，所以需要使用配准的方式对相机参数进行变换，还原出真实的坐标系。 </p><h1 id="二、相机阵配准"><a href="#二、相机阵配准" class="headerlink" title="二、相机阵配准"></a><strong>二、相机阵配准</strong></h1><h2 id="2-1、配准杆"><a href="#2-1、配准杆" class="headerlink" title="2.1、配准杆"></a><strong>2.1、配准杆</strong></h2><p>配准杆采用T字形红光灯，具体做法是将T形杆放置在地面并保持水平。T形杆如下图所示。</p><img src="/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/1.png" class=""><h2 id="2-2、配准杆红点检测"><a href="#2-2、配准杆红点检测" class="headerlink" title="2.2、配准杆红点检测"></a><strong>2.2、配准杆红点检测</strong></h2><p>水平放置配准杆后打开所有相机采集一张图像，并使用RGB相机动捕系列：相机标定（一、特征提取）这篇文章  ，<a href="https://suntfly.github.io/2024/01/05/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%80%E3%80%81%E7%89%B9%E5%BE%81%E6%8F%90%E5%8F%96%EF%BC%89/">链接</a><br>的红点提取方法对数据进行提取，下面是选取的3相机提取出来的红点示意图</p><img src="/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/2.png" class=""><h2 id="2-3、红点三维重建"><a href="#2-3、红点三维重建" class="headerlink" title="2.3、红点三维重建"></a><strong>2.3、红点三维重建</strong></h2><p>使用标定的相机参数重建T形杆的3D点。由于场景中需要重建的点较少，可以使用暴力重建方法直接重建3D数据。具体做法是：对特征数据进行两两匹配，通过相机内外参求解出基础矩阵，根据基础矩阵和极线的关系可筛选出满足条件的2D关系点，2D通过三角测量重建重建出3D点，最后利用2D点和3D点的关系融合3D点，得到最终需要的3D数据。这一部分后面我会单独写一篇博客进行说明，感兴趣的请随时关注。</p><h2 id="2-4、配准"><a href="#2-4、配准" class="headerlink" title="2.4、配准"></a><strong>2.4、配准</strong></h2><p>使用将原始坐标系的T字形标定杆与目标坐标系配准,再变换原始坐标系参数。下面是配准前后的相机阵和配准杆示意图。第一张图和第二张图中黄色的点为相机阵配准前的位置，分布在【0,1】空间。红色点为T形杆。第三张图绿色点为配准后的相机阵，红色点为配准杆。可以看出配准后相机参数已经还原到了真实坐标系的参数。配置算法使用最小费用最大流算法，将标准T形杆作为源点，测量得到的数据作为汇点，两点的距离作为费用。</p><img src="/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/3.png" class=""><p> 配准前相机阵和配准杆</p><img src="/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/4.png" class=""><p>配准前相机阵和配准杆放大</p><img src="/2024/05/11/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%89%E3%80%81%E7%9B%B8%E6%9C%BA%E9%98%B5%E9%85%8D%E5%87%86%EF%BC%89/5.png" class=""><p>配准后相机阵和配准杆</p><p>注。<a href="#fn1">1</a></p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p><hr>]]></content>
    
    
    <categories>
      
      <category>动捕标定</category>
      
    </categories>
    
    
    <tags>
      
      <tag>RGB相机动捕系列 相机标定</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RGB相机动捕系列：相机标定（二、标定计算）</title>
    <link href="/2024/04/02/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%BA%8C%E3%80%81%E6%A0%87%E5%AE%9A%E8%AE%A1%E7%AE%97%EF%BC%89/"/>
    <url>/2024/04/02/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%BA%8C%E3%80%81%E6%A0%87%E5%AE%9A%E8%AE%A1%E7%AE%97%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<p>**RGB相机动捕系列：相机标定（二、标定计算）</p><h1 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a><strong>一、概述</strong></h1><p>在上一篇博客中我们讲了相机标定的前置步骤，特征提取，本篇讲述后续的多相机标定中的标定计算部分；主要包括：1、匹配关联；2、几何验证；3、全局优化</p><h1 id="二、标定计算"><a href="#二、标定计算" class="headerlink" title="二、标定计算"></a><strong>二、标定计算</strong></h1><h2 id="2-1、匹配关联"><a href="#2-1、匹配关联" class="headerlink" title="2.1、匹配关联"></a><strong>2.1、匹配关联</strong></h2><p>红点提取完成后，可能存在同一时刻同一相机提取出了多个红点或者未提取到红点，则认为该帧为无效帧，将该帧删除，最后将有效帧进行两两匹配，构建相机-特征匹配关系。</p><h2 id="2-2、几何验证"><a href="#2-2、几何验证" class="headerlink" title="2.2、几何验证"></a><strong>2.2、几何验证</strong></h2><p>匹配关系构建完成后，需要去除误匹配，通过求解基础矩阵F和本质矩阵E进行几何验证（最后一次使用E矩阵），具体方法：首先根据2.1中得到的匹配关系求解基础矩阵F,常见的使用8点法求解基础矩阵，但由于噪声、误匹配等原因使用8点法求解基础矩阵不稳定，由于环境是人在动捕室中持续挥杆采集，以30帧的速度采集4分钟有7200帧，在实际使用中采集4分钟后经过上述2.1的匹配关联后最小的匹配对数量也大于500对，所以由于数据量充足，采用RANSAC（随机采样一致性）对基础矩阵进行估计，可以有效降低误匹配带来的误差。</p><h2 id="2-3、全局优化"><a href="#2-3、全局优化" class="headerlink" title="2.3、全局优化"></a><strong>2.3、全局优化</strong></h2><p>随着图像的不断增加，误差会不断累积，导致最后误差过大，将上述2.2中经过几何验证后的点进行全局优化消除误差。几何验证的匹配点对确保都是正确匹配，假设有N帧图像系列（每帧图像系由M个相机采集到的图像组成，假设每帧图像系列都有多个视图采集才标记点），于是最后的优化方程可以写成：</p><img src="/2024/04/02/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%BA%8C%E3%80%81%E6%A0%87%E5%AE%9A%E8%AE%A1%E7%AE%97%EF%BC%89/1.png" class=""><p>当摄像机i观察到轨迹j的时候Wij取1，反之取0，</p><p>||qij- P (C<sub>i</sub>, X<sub>j</sub>)||就是摄像机i中的轨迹j的投影误差累积和。</p><p>P(C<sub>i</sub>, X<sub>j</sub>),表示第j个三维点投影到相机i；</p><p>最优优化的输出结果包括N个三维点的坐标，以及M个相机的内外参数；</p><p>全局优化完成后，通过多次迭代上述2.3过程精确求解出相机的内外参。具体算法可以使用openMVG库实现。</p><p>下面是使用5相机标定的结果和报告，可以看出标定精度非常高，误差为0.8像素。</p><p>标定结果如图：</p><img src="/2024/04/02/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%BA%8C%E3%80%81%E6%A0%87%E5%AE%9A%E8%AE%A1%E7%AE%97%EF%BC%89/2.png" class=""><p>标定报告：</p><img src="/2024/04/02/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%BA%8C%E3%80%81%E6%A0%87%E5%AE%9A%E8%AE%A1%E7%AE%97%EF%BC%89/3.png" class=""><p>AC_RANSAC参考：<br>    [<a href="http://www.ipol.im/pub/art/2012/mmm-oh/">http://www.ipol.im/pub/art/2012/mmm-oh/</a>)<br>SFM参考：<br>    [<a href="https://github.com/openMVG/openMVG/wiki">https://github.com/openMVG/openMVG/wiki</a>)<br>上一篇：<br>    [<a href="https://suntfly.github.io/2024/01/05/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%80%E3%80%81%E7%89%B9%E5%BE%81%E6%8F%90%E5%8F%96%EF%BC%89/">https://suntfly.github.io/2024/01/05/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%80%E3%80%81%E7%89%B9%E5%BE%81%E6%8F%90%E5%8F%96%EF%BC%89/</a>)  </p><p>注。<a href="#fn1">1</a></p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p><hr>]]></content>
    
    
    <categories>
      
      <category>动捕标定</category>
      
    </categories>
    
    
    <tags>
      
      <tag>RGB相机动捕系列 相机标定</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RGB相机动捕系列：相机标定（一、特征提取）</title>
    <link href="/2024/03/10/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%80%E3%80%81%E7%89%B9%E5%BE%81%E6%8F%90%E5%8F%96%EF%BC%89/"/>
    <url>/2024/03/10/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%80%E3%80%81%E7%89%B9%E5%BE%81%E6%8F%90%E5%8F%96%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<p>**RGB相机动捕系列：相机标定（一、特征提取）</p><h1 id="一、概述"><a href="#一、概述" class="headerlink" title="一、概述"></a><strong>一、概述</strong></h1><p>在动捕行业中，动捕系统一般采用红外相机捕捉演员身上的mark红外反光点，如vicon、OptiTrack等相机，相机厂商会有配对的相机标定系统。但是红外光学动捕有时会有一定的局限性，比如对于手指的捕捉效果很差，所以有一套RGB相机动捕是很有必要的。但是市面上大多数RGB工业相机并没有配套的相机阵标定系统，本系列文章将阐述RGB相机动捕的整个系统，包括相机标定、相机配准、RGB相机身体动捕、RGB相机手指动捕。本文讲述RGB相机标定中的特征提取方法。</p><h1 id="二、相机标定"><a href="#二、相机标定" class="headerlink" title="二、相机标定"></a><strong>二、相机标定</strong></h1><h2 id="2-1、传统标定方法"><a href="#2-1、传统标定方法" class="headerlink" title="2.1、传统标定方法"></a><strong>2.1、传统标定方法</strong></h2><p>1、使用棋盘标定板进行标定，该方法首先对单个相机的内参进行标定，再对相机组的外参求解，使用该方法只能每次两两相机进行标定， 一般动捕室相机非常多，各相机之间的相对位姿关系也较为复杂，使用该方法需要消耗 大量人力物力，且该方法算法时间长，不利于使用。</p><p>2、使用显著标志物进行标定，该方法在场景中放置显著标志物，通过提取标志物的特征点进行匹配，实际使用中需要设计多个不同的标志物，标志物设计较为困难，特征提取受噪声影响较大，且标志物的放置需要有一定规则，需要保证同一标志物至少两相机可见，对于非专业人员操作不太友好。</p><h2 id="2-2、使用的标定方法"><a href="#2-2、使用的标定方法" class="headerlink" title="2.2、使用的标定方法"></a><strong>2.2、使用的标定方法</strong></h2><p>设计一个顶端使用 LED 红点光源的标定杆。标定时只需要人手持标定杆在动捕室进行挥动，相机通过波形发生器进行同步，通过算法分析实时提取出点光源在各个相机中的帧号和坐标，提取完成后再进行匹配关联，几何验证，最后进行全局优化，即可将相机内参外参一起标定完成，整个标定过程只需要几分钟就能完成大型动捕室相机阵列的高精度标定。</p><h2 id="2-3、特征提取"><a href="#2-3、特征提取" class="headerlink" title="2.3、特征提取"></a><strong>2.3、特征提取</strong></h2><p>该方法通过 LED 红灯作为标定杆，标定杆如下图所示：</p><img src="/2024/03/10/RGB%E7%9B%B8%E6%9C%BA%E5%8A%A8%E6%8D%95%E7%B3%BB%E5%88%97%EF%BC%9A%E7%9B%B8%E6%9C%BA%E6%A0%87%E5%AE%9A%EF%BC%88%E4%B8%80%E3%80%81%E7%89%B9%E5%BE%81%E6%8F%90%E5%8F%96%EF%BC%89/1.png" class=""><p>由操作人员手持标定杆在动捕室运动（运动规则是让尽可能多的点均匀的落在不同 相机上，这点很容易做到），通过红点提取算法计算出每帧图像中红点在各相机中的坐标，具体的红点检测算法如下： 特征提取：算法采用自定义特征：红率∙灰度，对于图像中的每个 RGB 像素值，特征 F 的计算公式为：</p><p>F&#x3D;reluR-G+relu(R-B)R+G+B+ε∙(0.3R+0.59G+0.11B)              （1）</p><p>其中𝜺是一个极小值，避免除0。 通过该自定义特征，能够很好的将红点特征提取出来，并且对于噪声也有很好的过滤。特征提取完成之后，通过 DOG（高斯函数的差分）算子对特征进一步处理，提取出点位置，图像中的高斯核函数 G 如下：</p><p>G（x,y,σ）&#x3D;12πσ2e(-x2+y22σ2)                                 （2）</p><p>其中𝝈是方差，代表斑点的尺度。 高斯差分结果 D 的计算公式为：</p><p>D（x,y,σ）&#x3D;G（x,y,σ+δ）-G（x,y,σ-δ）*F(x,y)                （3）</p><p>其中 G 为公式（2）中的高斯核函数，δ是一个极小量，用于构建尺度差分，在实际使用中取10−1，*是卷积运算，F 为公式（1）中的特征图。最后通过阈值判断当差分结果大于阈值时认为该点是红点.公式如下所示：</p><p>R&#x3D;0,  &amp;D（x,y,σ）&lt;threshold1,  &amp;D（x,y,σ）≥threshold                             （4） </p><p>注。<a href="#fn1">1</a></p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p><hr>]]></content>
    
    
    <categories>
      
      <category>动捕标定</category>
      
    </categories>
    
    
    <tags>
      
      <tag>RGB相机动捕系列 相机标定</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>TensorRT+CUDA加速人体关键点检测</title>
    <link href="/2022/06/10/TensorRT&amp;CUDA/"/>
    <url>/2022/06/10/TensorRT&amp;CUDA/</url>
    
    <content type="html"><![CDATA[<p>TensorRT+CUDA加速人体关键点检测</p><h4 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h4><ul><li><a href="#TensorRTCUDA_0">TensorRT+CUDA加速人体关键点检测</a></li><li><a href="#1_09_3">1、 人体关键点检测简介</a></li><li><a href="#2_09CPUGPU_17">2、 CPU和GPU的异同</a></li><li><a href="#3_09TensorRT_23">3、 TensorRT推理加速</a></li><li><a href="#4_09CUDA_42">4、 CUDA编程加速后处理</a></li></ul><h2 id="1、-人体关键点检测简介"><a href="#1、-人体关键点检测简介" class="headerlink" title="1、 人体关键点检测简介"></a>1、 人体关键点检测简介</h2><p>在影视游戏领域中，动画和角色中必须要有动作如奔跑、打斗、跳跃等，动捕技术出来之前的做法是动画师手工一帧帧修，此方法非常耗时，且非常考验动画师的经验。动捕技术常使用演员表演某些动作，并将这些动作转化为数字模型的动作，目前比较主流的几种动捕技术有红外动捕（标记点动捕）：演员穿着紧身衣，衣服上挂满红外反光点，通过红外相机获取反光点的运动轨迹来记录运动数据；惯性动捕：演员身上穿上各种陀螺仪进行记录运动数据；无标记动捕：不使用紧身衣，直接使用rgb相机获取图像，从图像中提取关节点来记录运动数据。上述几种方法各有优缺点，无标记动捕的优势在于演员不需要穿戴各种设备，能随意运动。  </p><img src="/2022/06/10/TensorRT&CUDA/1.png" class=""><img src="/2022/06/10/TensorRT&CUDA/2.png" class=""><img src="/2022/06/10/TensorRT&CUDA/3.png" class=""><p>使用无标记动捕中的第一个重要的步骤就是人体关键点检测，通过算法分析计算出图像中人体各关键点的位置。目前coco人体关键点数据集将人体关键点表示为17个关节，分别是鼻子、左右眼，左右耳，左右肩，左右肘，左右腕，左右臀，左右膝，左右脚踝。如下图所示  </p><img src="/2022/06/10/TensorRT&CUDA/4.png" class=""><img src="/2022/06/10/TensorRT&CUDA/5.png" class=""><p>目前主流的是使用深度学习方法计算出人体关键点位置，但是直接使用神经网络训练出的模型进行计算不够快，不能达到实时效果。由于人体关键点检测是整个无标记动捕过程中的第一步，它的效率直接影响整个流程的实时性，而实时是重要的，无论是作为表演时导演观察演员动作是否合理还是作为演员自身对动作细节的把握，都需要实时观察到整个动作的效果。所以得对整个模型进行加速，一种简单的做法是直接增加显卡的数量和计算能力，但此方法得耗费巨大资源购买显卡。另一种方法则是在软件层面上加速算法处理速度。可以使用了TensorRT和CUDA对整个运算过程进行加速，而这两种方法都是在GPU上进行运算，我们先来看看GPU和CPU有什么不同。</p><h2 id="2、-CPU和GPU的异同"><a href="#2、-CPU和GPU的异同" class="headerlink" title="2、 CPU和GPU的异同"></a>2、 CPU和GPU的异同</h2><p>GPU是graphics procession unit 的缩写，意味图形处理器，与之对应的一个概念是CPU，即center procession unit（中央处理器），一块典型的CPU拥有少数几个快速的计算核心，而一个典型的GPU拥有几百到几千个不那么快速的计算核心，如下图，黄色是控制器，绿色是算数逻辑单元，橙色是存储器，CPU和GPU之间通过PCIe总线连接通信。  </p><img src="/2022/06/10/TensorRT&CUDA/6.png" class=""><p>之所以如此设计，是由于其对应于不同的应用场景，CPU需要很强的通用性来处理各种不同的数据类型，同时又要逻辑判断引入大量的分支跳转和中断处理，所以CPU的更多晶体管用于数据缓存和流程控制。而GPU则是用于处理类型高度统一，相互无依存的大规模数据，所以更多的晶体管用于算数逻辑单元。</p><h2 id="3、-TensorRT推理加速"><a href="#3、-TensorRT推理加速" class="headerlink" title="3、 TensorRT推理加速"></a>3、 TensorRT推理加速</h2><p>tensorRT是基于python、C++、CUDA编写的一个库，其核心代码为C++和CUDA，用于优化经过训练的的深度学习模型以实现高性能推理。具体流程是将训练好的网络直接扔到tensorRT中，不再依赖深度学习框架（caffe、TensorFlow、pytorch等），如下图所示：  </p><img src="/2022/06/10/TensorRT&CUDA/7.png" class=""><p>tensorRT的性能优化主要有以下方面：  </p><img src="/2022/06/10/TensorRT&CUDA/8.png" class=""><p>权重激活与精度校准：大部分深度学习框架在训练神经网络时网络中的张量都是32位浮点型的，一旦网络训练完成，在部署推理的过程中不需要反向传播，完全可以适当降低数据的精度（如降低到INT8，相比于float32内存减少为1&#x2F;4），从而提高整体推理速度。虽说降低了数据的精度可能会导致推理结果出现偏差，但是只谈读毒性不谈剂量都是耍流氓，从实际测试中看int8和float差距并不大，可以满足大多数使用。<br>层与张量融合：制约计算速度的CUDA核心计算张量，往往大量的时间也是浪费在CUDA核心的启动和对每一层输入&#x2F;输出张量的读写操作上面，这造成了内存带宽的瓶颈和GPU资源的浪费。TensorRT通过对层间的横向或纵向合并，使得层的数量大大减少，这样就可以一定程度的减少kernel launches和内存读写。  </p><img src="/2022/06/10/TensorRT&CUDA/9.png" class=""><p>横向合并可以把卷积、偏置和激活层合并成一个CBR结构，只占用一个CUDA核心，纵向合并可以把结构相同但权值不同的层和并成一个更宽的层，也只占用一个CUDA核心，对于多分支合并情况，tensorRT完全可以实现直接到需要的地方，不做专门的concat处理，所以这一层也可以取消掉，合并之后如上图中的右边结构，层次更少、占用的CUDA的核心数也更少，因此整个模型结构会更小更快。<br>内核自动调整：根据不同的显卡结构、计算单元数量、内核频率等选择不同的优化策略以及计算方式，寻找最合适当前架构的计算方式，这种优化方法也带来了一定的缺点，经过优化的模型和GPU已经产生了绑定，在不同的GPU上优化的模型并不能兼容，但瑕不掩瑜，一般来说在动捕中整个框架都是事先确认好的，而额外增加或者跟换显卡设备也可以快速的从新推理生成模型。<br>动态张量显存：显存的开辟和释放比较耗时，tensorRT通过一些策略减少显存的开辟和释放次数，增加显存重复利用率从而减少时间。<br>多流执行：使用CUDA中的stream操作，可以并行处理数据，从而提高显卡的利用率。<br>通过将训练好的模型进行tensorRT加速优化，并序列化到磁盘中，使用时通过反序列化即可使用优化后的推理引擎加速。使用tensorRT加速的效果如何，通过对比pytorch和tensorRT，均在1080Ti显卡上执行，在pytorch中的计算时间为55毫秒，使用tensorRT加速后的计算时间为10.5毫秒，速度提高了5.23倍。</p><h2 id="4、-CUDA编程加速后处理"><a href="#4、-CUDA编程加速后处理" class="headerlink" title="4、 CUDA编程加速后处理"></a>4、 CUDA编程加速后处理</h2><p>通过tensorRT优化加速后整个流程并没有结束。首先来看一下关键点回归ground truth的构建问题，主要有coordinate和heatmap两种。Coordinate直接将关键点的坐标作为网络最后的回归目标，使用coordinate之后则不需要再做后续处理，直接将坐标输出给后续流程，但是coordinate回归的是关键点对于图片的offset，而长距离的offset在实际学习过程中很难回归，会造成较大的误差，且在训练过程中提供的监督信息较少，网络收敛较慢。使用heatmap将每一个坐标用一个概率图来表示，对于图片中每个像素都给定一个概率，表示该点属于对应类别关键点的概率，距离关键点越近的概率越趋近1，否则越趋近0，每一个点都设置了监督信息，收敛较快，且对每个像素都提供了预测能有效提高精度。使用heatmap输出的结果并不是单一的坐标，而是一张图像像，如下图所示，整个图像的宽高和输入尺寸一致，17个关键点则有17个维度，需要从这些图像中提取坐标使用CPU计算非常耗时，现在没有了tensorRT这神器帮我们优化了，那么就得靠自己手撸CUDA代码。</p><img src="/2022/06/10/TensorRT&CUDA/10.png" class=""><p>CUDA是NVIDIA公司发布的一种操作GPU计算的硬件和软件架构，下图是CUDA的软件架构，开发应用程序以主机（CPU）为出发点，应用程序可以调用CUDA运行时API、CUDA驱动API以及一些已有的CUDA库。所有这些调用都将利用设备（GPU）的硬件资源。  </p><img src="/2022/06/10/TensorRT&CUDA/11.png" class=""><p>CUDA编程是一个异构模型，不能单独在GPU中运行，必须在和CPU协同工作，在CUDA中有host（CPU及其内存）和device（GPU及其内存）两个重要概念，前面介绍了CPU和GPU的异同，所以对于较多逻辑判断、分支跳转时使用CPU，需要处理大量简单重复的数据使用GPU，并通过PCIe总线进行通信。以处理关键点heatmap为例，第一步需要将heatmap缩放到输入尺寸，具体流程：（1）、得到tensorRT推理之后的数据，由于tensorRT推理后的数据本身就存在显存中，所以不需要再重新复制，直接可以将数据指针传入到处理函数中；（2）、传入之后根据缩放图像大小开辟等量的数据，在cuda中使用&lt;&lt;&lt;grid,block&gt;&gt;&gt;配置线程数。（3）、每个线程单独处理每个像素值计算出当前位置缩放的值，使程序在逻辑上处于并行状态。<br>上面步骤中grid表示线程块数，block表示每个线程块内的线程数，所以总大小为grid*block个线程， 之所以说在逻辑上是并行的，是由于GPU和CPU类似，多线程如果没有多核支持，在物理层上无法实现并行。但GPU有很多CUDA核心，GPU硬件的一个核心组件是SM(流式多处理器)，SM的核心组件包括CUDA核心、共享内存、寄存器等，SM可以并行的执行数百个线程。所以，上述流程被执行时，grid中的线程块被分配到SM上，SM中以线程束作为基本执行单元，目前所以GPU中线程束大小都是32，所以物理上并行的最大线程数为32个线程。<br>Heatmap经过缩放，非极大值抑制等一系列处理最终输出得到关键点精确坐标，对比CUDA编程和CPU编程，在CPU（i7 3.2GHz）上处理整个过程消耗25毫秒，在CUDA（1080Ti显卡）中处理消耗2.3毫秒,提升超过了10倍。</p><p>注。<a href="#fn1">1</a></p><p>本文仅供学习参考，未经允许，拒绝转载或者用作他用。</p><hr><ol><li>参考连接<br><a href="https://developer.nvidia.com/zh-cn/tensorrt">https://developer.nvidia.com/zh-cn/tensorrt</a><br><a href="https://zhuanlan.zhihu.com/p/102457223">https://zhuanlan.zhihu.com/p/102457223</a><br><a href="https://zhuanlan.zhihu.com/p/69042249">https://zhuanlan.zhihu.com/p/69042249</a><br><a href="https://zhuanlan.zhihu.com/p/371663018">https://zhuanlan.zhihu.com/p/371663018</a> <a href="#fnref1">↩︎</a>## 目标</li></ol><h3 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h3>]]></content>
    
    
    <categories>
      
      <category>TensorRT</category>
      
    </categories>
    
    
    <tags>
      
      <tag>TensorRT CUDA</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>测试文章</title>
    <link href="/2021/06/10/%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/"/>
    <url>/2021/06/10/%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/</url>
    
    <content type="html"><![CDATA[<p>这是一篇测试文章</p><img src="/2021/06/10/%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/test.png" class="">]]></content>
    
    
    
    <tags>
      
      <tag>Test</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
